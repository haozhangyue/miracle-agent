# 22_P3前端架构与工作台状态设计

> 文档状态：P3 前端架构详细设计。
>
> 设计输入：P2 Web 工作台原型、P2F-07 Agent Collaboration、`19-21` 数据模型和 API 设计。

## 1. 前端目标

P3 前端设计目标：

1. 将 P2 原型转为可实现的页面、组件和状态结构。
2. 保持领域可扩展，不把 `content-production` 写死进 UI。
3. 让不同 DomainPack 和 RoleProfile 决定页面默认视角。
4. 保持 RunSpec 和 WorkflowSnapshot 只读、Event 追加、Artifact 版本化的认知。
5. 为 Local Sidecar MVP 和未来 Cloud API 共用接口形态。

## 2. 技术基线

| 层级 | MVP 方案 | 说明 |
|---|---|---|
| 框架 | React + Vite + TypeScript | 沿用 `14` 技术选型。 |
| 路由 | React Router | 页面路由和深链接。 |
| 请求 | fetch wrapper / TanStack Query 可选 | MVP 可先用轻量封装，后续引入 query cache。 |
| UI 状态 | Zustand 或轻量 store | 管理当前 Run、选中节点、Attention、Agent 过滤器。 |
| 图标 | lucide-react | 原型已使用，正式实现统一尺寸和颜色 token。 |
| DAG | React Flow / XYFlow | P3 只设计接口，MVP 可先实现只读 Node DAG。 |
| Canvas | tldraw | MVP 后段做空间组织原型，不承担严肃执行编辑。 |

P3 不要求当前 `prototypes/p2/fusion-clickable` 直接变成生产代码，但页面结构和视觉决策可作为实现基线。

## 3. 路由设计

推荐路由：

```text
/
/new
/dry-run/:workflowId
/runs
/runs/:runId
/runs/:runId/nodes/:nodeRunId
/attention
/attention/:attentionId
/agents
/agents/:agentId
/artifacts
/artifacts/:artifactId
/gates/:gateInstanceId
/workflows
/workflows/:workflowId
/registry
/settings
```

路由原则：

1. `runId` 是运行上下文主轴。
2. `nodeRunId`、`attentionId`、`artifactId`、`gateInstanceId` 都支持深链接。
3. 页面 Tab 不改变对象事实，只改变视图投影。
4. RoleProfile 决定默认 landing，但不改变 URL 语义。

## 4. 页面模块

### 4.1 首页

目标：

```text
用户进入后立刻知道下一步处理什么
```

数据来源：

| 区块 | API | 对象 |
|---|---|---|
| 待我处理 | `/attention` | AttentionItem |
| 继续运行 | `/runs` | RunSummary |
| 快速启动 | `/domains`、`/registry/templates` | DomainPack、WorkflowTemplate |
| 最近交付 | `/artifacts` | ArtifactManifest |
| 系统风险 | `/attention` | AttentionItem |

扩展规则：

1. DomainPack 可以提供首页默认模板排序。
2. RoleProfile 可以改变首页主区块，例如 editor 默认强化审核和 Artifact。
3. 首页不得硬编码 Flow A-G，只显示模板和运行摘要。

### 4.2 新任务 / Dry-run

目标：

```text
用户选择领域、模板、输入和执行策略，启动前看到风险
```

状态：

```text
selectedDomain
selectedTemplate
taskInput
workflowSelectionMode
executionPolicy
dryRunResult
credentialRisks
gateRisks
providerOptions
```

关键规则：

1. 工作流选择方式是：推荐模板 / 从模板自定义 / AI 生成草案。
2. 执行策略是：Auto / Manual / Hybrid。
3. 二者不能混用。
4. Dry-run 必须展示缺失凭证、审核门、provider、成本、分支影响。
5. 用户确认启动时调用 `POST /runs`，由 Sidecar 创建 RunSpec、冻结 WorkflowSnapshot 并返回 `run_id`。

### 4.3 Run 工作区

目标：

```text
用户看懂当前 Run 执行到哪里、哪个节点异常、下游影响是什么
```

布局：

```text
Run Header
-> Fixed View Tabs
-> Dynamic Stage Filter
-> Node DAG / Node List
-> Node Context Panel
-> Event Drawer
```

数据来源：

| 区块 | API |
|---|---|
| Run Header | `/runs/:id` |
| Node DAG | `/runs/:id` |
| Node Detail | `/runs/:id/nodes/:nodeRunId` |
| Event Drawer | `/runs/:id/events` |
| Attention Count | `/attention?run_id=...` |

状态：

```text
currentRunId
selectedView
selectedStage
selectedNodeRunId
eventDrawerOpen
nodeDetailOpen
```

规则：

1. RunSpec 和 WorkflowSnapshot 只读。
2. 编辑结构只能创建 Workflow draft。
3. NodeRun 状态和 NodeAttempt 状态分开展示。
4. 阶段过滤器来自 WorkflowSnapshot layout，不参与执行依赖。
5. 未来 DomainPack 可以改变阶段名，但不能改变运行事实。

### 4.4 Attention

目标：

```text
一个根因对应一个主 Attention Item，用户看到影响对象和安全动作
```

数据来源：

| 区块 | API |
|---|---|
| Attention Queue | `/attention` |
| Attention Detail | `/attention/:id` |
| Related Objects | `/attention/:id` |
| Safe Actions | `/attention/:id/actions` |
| Agent Situation | `/agents/collaboration` |

状态：

```text
selectedAttentionId
attentionStatusFilter
severityFilter
sortMode
expandedItemIds
safeActionDialog
```

规则：

1. Attention 状态：open、acknowledged、snoozed、resolved。
2. 静音是通知动作，不是恢复动作。
3. 跳过分支必须显示影响范围和不受影响路径。
4. resolved 进入历史，不直接删除。

### 4.5 Agent Collaboration

目标：

```text
用户看到 Agent 职责、状态、等待对象、交接产物和阻塞传播
```

数据来源：

| 区块 | API |
|---|---|
| Agent Health | `/agents/health` |
| Collaboration Links | `/agents/collaboration` |
| Run Context | `/runs/:id` |
| Related Attention | `/attention?run_id=...` |

状态：

```text
selectedAgentId
agentStatusFilter
showBlockedOnly
selectedHandoffId
```

规则：

1. Agent 可以同时参与多个 Run。
2. UI 可以突出当前 Run，但模型不能假设单任务。
3. Agent 交接必须绑定 Artifact 或 Gate。
4. AgentHealth 是投影，不是 AgentSpec。

### 4.6 审核抽屉

目标：

```text
用户审核产物，批准或驳回都保留历史
```

数据来源：

| 区块 | API |
|---|---|
| Artifact Detail | `/artifacts/:id` |
| Gate Detail | `/gates/:id` |
| Decision | `/gates/:id/decision` |

规则：

1. 审核不覆盖 Artifact。
2. 驳回创建新 NodeAttempt 或新 Artifact version。
3. GateDecision 记录 actor、decision、comment、timestamp。
4. 高风险审核不得被自动静音或隐藏。

## 5. Store 设计

MVP 推荐拆分：

| Store | 职责 |
|---|---|
| `appStore` | 当前 workspace、role、domain、全局错误。 |
| `registryStore` | DomainPack、WorkflowTemplate、ComponentLibrary 缓存。 |
| `runStore` | 当前 Run、节点选择、阶段过滤、事件抽屉。 |
| `attentionStore` | Attention 列表、选中项、筛选、动作确认。 |
| `agentStore` | AgentHealth、协作链路、选中 Agent。 |
| `artifactStore` | Artifact 列表、预览、版本。 |

原则：

1. Server state 与 UI state 分开。
2. 写动作必须经过 API，不直接改 store 伪造成功。
3. Event 返回后再更新投影。
4. Store 不保存隐藏推理链和密钥。

## 6. 数据加载策略

首页：

```text
并行加载 runs、attention、templates、artifacts
```

Run：

```text
先加载 run manifest
-> 再加载 node detail / events / attention
```

Attention：

```text
先加载 queue
-> 选择 item 后加载 detail 和 related objects
```

Agent：

```text
加载 health projection
-> 根据 run_id 加载 collaboration links
```

错误处理：

| 错误 | UI 表现 |
|---|---|
| missing_credential | Attention + 恢复动作。 |
| validation_error | Dry-run 风险表。 |
| provider_failure | Node detail + fallback。 |
| filesystem_error | 顶部系统风险 + 重试。 |
| conflict | Spec diff/合并提示。 |
| permission_denied | 操作禁用 + 权限说明。 |

## 7. RoleProfile 投影

RoleProfile 不改变数据，只改变默认视图。

示例：

| RoleProfile | 首页优先区块 | Run 默认 Tab | Attention 默认过滤 |
|---|---|---|---|
| operator | 待我处理、继续运行 | 流程 | open + P0/P1 |
| editor | 审核、最近交付 | 产物 | pending_review |
| designer | 视觉资产、任务输入 | 产物 | image/provider |
| researcher | 资料、证据、报告 | 审计 | citation/conflict |
| manager | 成本、风险、进度 | 总览 | P0/P1 |
| developer | Spec、Trace、Adapter | 审计 | provider/filesystem |

MVP 可以先使用 `operator` 默认视角，但实现不应写死。

## 8. DomainPack UI 扩展点

DomainPack 可以配置：

```yaml
ui:
  home_sections:
    - attention
    - active_runs
    - quick_launch
    - recent_artifacts
  artifact_board:
    default_group_by: artifact_type
  run:
    stage_labels:
      source: 采集
      transform: 生成
      review_gate: 审核
  attention:
    severity_order:
      - P0
      - P1
      - P2
```

UI 读取配置，但不能让 DomainPack 修改核心状态机。

## 9. 组件拆分建议

核心组件：

| 组件 | 用途 |
|---|---|
| `AppShell` | 侧栏、顶部栏、workspace、全局健康。 |
| `HomeActionTable` | 首页待处理。 |
| `RunSummaryTable` | 首页继续运行。 |
| `WorkflowQuickLaunch` | 模板启动。 |
| `RunHeader` | Run 状态、快照、成本、Attention。 |
| `RunStageFilter` | 动态阶段过滤。 |
| `NodeRunGraph` | DAG / 节点列表。 |
| `NodeContextPanel` | 右侧节点详情。 |
| `EventDrawer` | 事件审计。 |
| `AttentionQueue` | 根因队列。 |
| `AttentionDetail` | 关联对象和安全动作。 |
| `AgentCollaborationMap` | Agent 协作态势。 |
| `GateReviewDrawer` | 审核抽屉。 |
| `ArtifactPreview` | 产物预览。 |

## 10. 可访问性和实现验收

P3 实现验收必须覆盖：

1. 所有按钮可键盘聚焦。
2. Tab、Drawer、Dialog 有 ARIA 语义。
3. running、blocked、resolved 状态变化有文本反馈。
4. DAG/Canvas 提供列表替代视图。
5. Attention 数量变化不抢焦点。
6. 颜色状态必须有文字标签。
7. 长 artifact 名称不撑破布局。

## 11. 前端验收场景

1. 用户进入首页，10 秒内能找到最高优先级问题。
2. 用户从首页进入 Run，能定位 blocked 节点。
3. 用户从 Run 进入 Attention，能看到根因和关联对象。
4. 用户进入 Agent Collaboration，能看到哪个 Agent 等待哪个产物。
5. 用户从审核抽屉驳回产物，UI 提示将创建新版本而不是覆盖旧产物。
6. 切换 DomainPack 后，首页和模板入口变化，但核心页面仍可工作。

# 13_P0 架构评审纪要与决策清单

## 1. 评审目标

本文件用于固化 Miracle P0 阶段的架构评审结论、关键决策、待决事项和下一阶段安排。

P0 不进入代码实现，重点回答 4 个问题：

1. Miracle 的系统边界是否清晰。
2. Spec 优先、多 Agent 可视化、双模式编排是否作为核心路线。
3. MVP 是否能从“热点工具更新”真实工作流开始落地。
4. 技术架构选型、技术架构图、产品设计图应在哪个阶段完成。

## 2. 评审输入

| 输入 | 用途 |
|---|---|
| `00_Miracle奇迹系统总体规划设计.md` | 确认总体定位、系统分层和第一条样本。 |
| `01_核心架构与对象模型.md` | 确认 Workflow、Node、Component、Agent、Provider、Trace 等核心对象。 |
| `02_组件库与插件体系设计.md` | 确认组件库、tool、skill、MCP、provider、API 的组合方式。 |
| `03_智能路由与工作流编排设计.md` | 确认 auto/manual/hybrid、子工作流、DAG 和路由体系。 |
| `04_多Agent协同与可视化设计.md` | 确认 Agent 状态、协作关系、执行链路和产物流转。 |
| `05_双模式工作流可视化编排设计.md` | 确认无限画布和流程节点模式的统一 graph 设计。 |
| `06_智能进化体系设计.md` | 确认复盘、评估、推荐和版本升级闭环。 |
| `07_后续对接路线图与任务拆解.md` | 确认后续阶段和 MVP 顺序。 |
| `08_Miracle竞品分析与架构借鉴报告.md` | 确认竞品吸收设计和战略定位修订。 |
| `09_WorkflowSpec与Registry技术草案.md` | 确认 WorkflowSpec、Registry、validate、dry-run、estimate 的雏形。 |
| `10_AgentHealth与多Agent状态机设计.md` | 确认 AgentHealth、PermissionMatrix 和多 Agent 状态机。 |
| `11_VisualBuilder与Spec双向同步设计.md` | 确认 UI、YAML/JSON 和远端模板之间的同步规则。 |
| `12_MVP原型功能清单与界面草图.md` | 确认 MVP 功能顺序和界面验证范围。 |

## 3. P0 已确认决策

| 决策项 | P0 结论 | 原因 |
|---|---|---|
| 系统定位 | Miracle 定位为面向内容生产、媒体资产和复杂智能任务的本地优先 Agent OS。 | 避免只做单一工作流工具，保留未来扩展到生图、生视频、剧本、漫剧、研究和运营的空间。 |
| 架构方法 | 采用 Spec 优先。 | 工作流必须可版本化、可 diff、可回滚、可进入 Git 管理。 |
| 第一条样本 | 以“热点工具更新”Flow A-G 作为第一条真实工作流。 | 该流程已有采集、核验、母稿、脚本、分镜、TTS、视频、分发、复盘等完整链路。 |
| 工作流模型 | `WorkflowSpec` 是模板与编排真相。 | 无限画布、DAG、YAML/JSON 都只是同一套 spec 的不同编辑器；运行事实由 RunSpec、NodeRun、TraceEvent、ArtifactManifest、GateDecision 承载。 |
| 组件模型 | 节点负责流程语义，组件库封装 skill/tool/provider/agent/prompt/MCP/API 组合。 | 降低用户自由组合成本，同时保留扩展能力。 |
| 可视化优先级 | 先做 Node DAG，再做 Agent Collaboration，再做 Artifact Board，最后做 Infinite Canvas。 | 先保证执行链路可解释，再增强空间创作体验。 |
| 多 Agent 管理 | AgentHealth、PermissionMatrix、状态流转进入 MVP 设计。 | Miracle 的核心价值之一是让用户看清谁在执行、谁在等待、谁被阻塞。 |
| Runtime Adapter | Codex、Hermes、OpenClaw、官方 API 统一进入 Adapter 抽象。 | 不绑定单一智能体工具，为后续平台替换、路由和降本保留空间。 |
| 审核门 | 高风险节点必须保留 GateReview，不允许 Auto 模式静默越过。 | 内容事实、发布、视频渲染、外部 API 调用都需要可审计。 |
| 进化机制 | 第一版只做 Evolution Board 和建议，不自动覆盖 stable 工作流。 | 让系统能学习，但不牺牲稳定性和可控性。 |

## 4. 关键问题结论

### 4.1 技术架构选型安排在哪里

当前 `07_后续对接路线图与任务拆解.md` 中把“前端框架、画布/节点图方案、本地数据存储、后端或本地服务形态”等技术选型放在 `P3 技术详细设计`，这个安排偏晚。

P0 评审修订结论：

```text
P0 架构评审
-> P1 Spec 与接口方案技术设计
-> P1.5 技术架构选型与系统架构图
-> P2 产品信息架构与原型设计
-> P3 技术详细设计与实施拆解
-> P4 MVP 实现
-> P5 真实工作流接入与进化
```

技术架构选型应放在 `P1.5`，位于 Spec/接口方案之后、产品原型和详细技术设计之前。

原因：

- 没有 Spec 和对象模型，技术选型容易变成框架偏好讨论。
- 没有技术架构选型，产品原型容易设计出实现成本过高的交互。
- 没有系统架构图，后续 Adapter、Registry、Trace、Canvas 和本地存储边界会反复摇摆。

### 4.2 技术架构图安排在哪里

技术架构图不应放到 P3 才产出，应在 `P1.5 技术架构选型与系统架构图` 中产出。

建议新增文档：

```text
14_技术架构选型与系统架构图.md
```

该文档至少包含：

1. 总体系统架构图。
2. 本地优先部署形态图。
3. WorkflowSpec 到 RunTrace 的数据流图。
4. Runtime Adapter 调用链路图。
5. Registry、ComponentLibrary、ProviderRouter 的关系图。
6. 前端、后端、本地文件、Git、数据库之间的边界图。
7. 技术选型对比表和推荐结论。

### 4.3 产品设计图安排在哪里

产品设计图应放在 `P2 产品信息架构与原型设计`，而不是只写在 MVP 功能清单里。

建议新增文档：

```text
16_产品信息架构与设计图规划.md
```

该文档至少包含：

1. 产品信息架构图。
2. 核心页面地图。
3. 用户任务启动流程图。
4. Workflow Builder 双模式切换流程图。
5. Agent Collaboration Dashboard 页面结构图。
6. Artifact Board 页面结构图。
7. Gate Review 审核返工流程图。
8. MVP 低保真线框说明。

### 4.4 是否现在就定具体技术栈

P0 不直接定最终技术栈，但必须明确“技术栈评审将在 P1.5 独立发生”。

P1.5 可讨论候选方向：

| 层级 | 候选项 | P1.5 需要回答 |
|---|---|---|
| 前端应用 | React、Next.js、Vite、Electron/Tauri、本地 Web App | 第一版是纯本地 Web、桌面壳，还是未来可云化的 Web 架构。 |
| 节点 DAG | React Flow、XYFlow、Rete、LiteGraph、自研轻量 DAG | 是否能支持节点状态、审核门、子工作流、运行态高亮。 |
| 无限画布 | tldraw、Excalidraw、Fabric/Konva、自研 canvas layer | 是否能承载任务卡、素材卡、产物卡、区域和版本分支。 |
| 本地存储 | YAML/JSON 文件、SQLite、DuckDB、混合方案 | 第一版 trace、artifact、registry 如何查询和版本化。 |
| 后端形态 | Node 本地服务、Python 本地服务、无后端静态读取、混合 CLI | 如何兼容 Codex/Hermes/OpenClaw/API adapter。 |
| 运行时集成 | CLI adapter、MCP adapter、HTTP API adapter、官方 SDK | 节点执行、日志、凭证和错误如何统一记录。 |
| 图表与时间线 | Mermaid、D3、vis-timeline、TanStack Table | 哪些用于文档，哪些用于产品 UI。 |

## 5. P0 决策清单

| ID | 决策 | 状态 | 后续动作 |
|---|---|---|---|
| D001 | Miracle 采用本地优先 Agent OS 定位。 | 已确认 | README 与 00-15 已体现。 |
| D002 | WorkflowSpec 是工作流模板与编排真相，运行事实单独分层。 | 已修订 | P1/P1.5 继续细化 RunSpec、NodeRun、TraceEvent、ArtifactManifest、GateDecision。 |
| D003 | Registry 进入核心架构，不作为后续增强项。 | 已确认 | P1 补齐 Registry schema 和版本策略。 |
| D004 | AgentHealth 和 PermissionMatrix 进入 MVP 设计。 | 已确认 | P2 原型必须展示健康卡和阻塞恢复动作。 |
| D005 | Node DAG 优先于 Infinite Canvas 实现。 | 已确认 | MVP 顺序保持 MVPS04 在 MVPS08 之前。 |
| D006 | Infinite Canvas 保留为核心差异化能力。 | 已确认 | P2 做可交互原型，P4 可先做轻量版本。 |
| D007 | Visual/Spec Sync 不允许静默覆盖 stable workflow。 | 已确认 | P1/P1.5/P2 均需保留冲突策略。 |
| D008 | Auto 模式只能推荐和草案化，不自动发布高风险变更。 | 已确认 | Evolution Board 默认产出候选建议。 |
| D009 | 技术架构选型从 P3 前置到 P1.5。 | 已确认 | 修订 `07`，新增 `14` 作为下一步文档。 |
| D010 | 产品设计图从 MVP 清单中拆出为 P2 专项。 | 已确认 | 新增 `16` 作为产品 IA 和设计图文档。 |
| D011 | P0 后不直接写代码工程。 | 已确认 | 先完成 P1、P1.5、P2 评审。 |
| D012 | GitHub 仓库继续作为文档资产和未来 schema/prototype 的版本管理中心。 | 已确认 | 每轮文档资产完成后提交。 |

## 6. 待决问题

| ID | 问题 | 建议处理阶段 | 决策口径 |
|---|---|---|---|
| O001 | 第一版是浏览器本地 Web、桌面应用，还是本地服务 + Web UI？ | P1.5 | 结合文件权限、MCP/CLI 调用、用户启动成本决定。 |
| O002 | Infinite Canvas 使用成熟开源画布库还是自研轻量层？ | P1.5 | 先以对象模型和双向同步能力为核心，不以视觉自由度优先。 |
| O003 | Trace/Event 存储使用 JSONL 文件还是 SQLite？ | P1.5 | 需要同时考虑 Git 可读性、查询性能和历史 run 数量。 |
| O004 | 官方 API、Codex、Hermes、OpenClaw 的 adapter 是否共用同一 RunEvent 协议？ | P1/P1.5 | 建议共用事件协议，差异收敛到 adapter metadata。 |
| O005 | 产品原型先做高保真还是低保真可点击？ | P2 | 建议先低保真验证信息密度，再做高保真。 |
| O006 | 真实执行器是否进入第一版 MVP？ | P3/P4 | 建议第一版先只读历史 run + 手动 run draft，再逐步接入执行器。 |

## 7. P0 后立即行动

推荐后续顺序：

1. 修订 `07_后续对接路线图与任务拆解.md`，加入 `P1.5 技术架构选型与系统架构图`。
2. 新增 `14_技术架构选型与系统架构图.md`，开始技术栈、架构图和数据流图讨论。
3. 新增 `16_产品信息架构与设计图规划.md`，开始产品页面地图和核心交互图讨论。
4. 再回到 `09-12`，把 Spec、AgentHealth、Visual Sync、MVP 页面结构和 `14/15/16` 对齐。
5. P1/P1.5/P2 都评审通过后，再进入 P3 技术详细设计和任务拆解。

## 8. P0 验收标准

P0 通过的标准：

- Miracle 的定位、第一条样本、核心对象和 MVP 顺序已确认。
- 技术架构选型不再隐藏在 P3，已前置为 P1.5 独立阶段。
- 技术架构图有明确文档承载位置。
- 产品设计图有明确文档承载位置。
- P1、P1.5、P2、P3 的边界清晰，不再把方案、选型、原型和详细实现混在一起。

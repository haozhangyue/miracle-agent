# P5-07 半自动新 Run 草案设计

> 文档状态：CURRENT
>
> 阶段结论：P5-07 已完成；下一步进入 P5-08 首个真实 Adapter 边界评估。
>
> 适用范围：真实“热点工具更新”工作流的新 Run 启动准备，不调用真实 Runner。

## 1. 目标与边界

P5-07 把 Miracle 从“历史 Run 只读展示”推进到“可创建、可校验、可人工确认的新 Run
草案”。本阶段只准备执行事实，不产生执行事实。

必须满足：

1. 以 `content-production-real-v0` 创建新的 Run 草案。
2. 固化输入、WorkflowSnapshot 草案、组件候选、Provider 候选、审核门和可选分支。
3. Dry-run 展示凭证、审核、成本、时长、风险和降级影响。
4. 用户确认前不创建正式 `RunSpec`、`NodeRun` 或运行 `TraceEvent`。
5. 用户确认后也只生成待启动请求；真实执行必须等待 P5-08 Adapter 边界落地。

本阶段不做：

- 不调用 Codex、官方模型 API、TTS、Pencil 或 HyperFrames。
- 不执行 Flow A-G 任一真实节点。
- 不把 `RunDraft` 提升为新的运行根对象；正式运行根对象仍是 `RunSpec`。
- 不把启动确认建模为 `GateInstance`；工作流 Gate 只在正式 Run 内存在。
- 不把草案审计记录写入正式 Run Event Journal。

## 2. 对象真相边界

| 对象 | 阶段 | 是否运行真相 | 用途 |
|---|---|---:|---|
| `RunDraft` | 启动前 | 否 | 保存用户意图、输入引用和候选解析结果。 |
| `WorkflowSnapshotDraft` | 启动前 | 否 | 预览即将冻结的 WorkflowSpec 副本。 |
| `DryRunPlan` | 启动前 | 否 | 复用 `packages/core` 现有校验与节点计划契约。 |
| `RunDraftDryRunPlan` | 启动前 | 否 | 在核心 DryRunPlan 外封装草案、路由、分支影响和确认哈希。 |
| `LaunchConfirmation` | 启动前 | 否 | 记录用户是否同意按当前计划进入正式启动。 |
| `RunSpec` | 正式运行 | 是 | 用户确认后由 Orchestrator 创建的运行根对象。 |
| `WorkflowSnapshot` | 正式运行 | 是 | 与 RunSpec 同时冻结的不可变工作流副本。 |
| `TraceEvent` | 正式运行 | 是 | 仅记录正式 Run 的执行、审核和恢复事实。 |

`RunDraft` 是启动会话对象，不是 `RunSpec` 的另一种名称。它可以被修改、重新 Dry-run、
取消或过期；`RunSpec` 一经创建只能通过事件和投影演进。

## 3. RunDraft v0

```yaml
draft_id: rundraft_20260710_001
status: needs_review
mode: semi_automatic
workflow_ref:
  workflow_id: content-production-real-v0
  version: 0.1.0
domain_pack_id: content-production
role_profile_id: content-operator
created_from:
  type: workflow_template
  source_id: content-production-real-v0
operator_intent:
  title: 制作一期 AI 工具更新内容包
  goal: 生成可审核的 MD 母稿、分发包和可选视频包
inputs:
  topic_brief:
    kind: inline_text
    required: true
    value: Codex 与 Claude Code 最新动态
  source_scope:
    kind: config_ref
    value: official-and-authoritative
execution_policy: hybrid
branch_selections:
  markdown_distribution: required
  video_package: optional
  prototype_pencil: disabled
workflow_snapshot_draft_ref: drafts/rundraft_20260710_001/workflow_snapshot_draft.json
dry_run_plan_ref: drafts/rundraft_20260710_001/run_draft_dry_run_plan.json
launch_confirmation_ref: drafts/rundraft_20260710_001/launch_confirmation.json
created_at: 2026-07-10T10:00:00+08:00
updated_at: 2026-07-10T10:00:00+08:00
expires_at: 2026-07-11T10:00:00+08:00
```

### 3.1 状态机

```text
draft
  -> validating
  -> invalid
  -> needs_review
  -> ready_for_confirmation
  -> confirmed
  -> launch_pending
  -> converted

draft / invalid / needs_review / ready_for_confirmation
  -> cancelled

任一未转换状态
  -> expired
```

规则：

- `invalid`：存在无法形成计划的结构错误，例如引用不存在。
- `needs_review`：计划已形成，但存在缺凭证、高风险或人工选择项。
- `ready_for_confirmation`：所有启动方案选择已确定；仍未具备的 Adapter 等启动能力已被
  明确列为待解决项，允许用户确认当前计划但不代表已经可以启动。
- `confirmed`：用户签署了当前 `plan_hash`，但尚未创建正式 Run。
- `launch_pending`：正式启动命令已被接受并获得 `operation_id`，等待原子创建 Run。
- `converted`：正式 `RunSpec` 创建成功，草案只读保留。
- 草案输入、分支或 Provider 选择变化后，原确认自动失效并重新 Dry-run。

## 4. WorkflowSnapshotDraft

`WorkflowSnapshotDraft` 从稳定的 `content-production-real-v0` 解析而来，至少包含：

- Flow A-G 通用 NodeSpec 和 EdgeSpec。
- C、D、F 阶段审核引用；F 阶段保持最终人工审核。
- MD 分发必选主链和视频可选分支。
- resolved component candidates 和 Provider candidates。
- 输入引用、产物计划和所需能力。
- 原始 WorkflowSpec 的 `source_hash` 与草案 `snapshot_hash`。

布局数据可以保留用于预览，但不参与执行依赖。正式启动时，Orchestrator 必须重新验证
`source_hash`；源 Workflow 已变化则拒绝转换，并要求重新生成 Dry-run。

## 5. RunDraftDryRunPlan v0

`RunDraftDryRunPlan` 是启动草案封装，不替代 `packages/core` 已有 `DryRunPlan`。其中
`core_plan` 必须保持现有 `DryRunPlan` 的字段和语义；草案专属字段只放在外层。后续实现
若扩展核心计划，应通过兼容字段或新版本 schema 演进，不能再创建第二个同名类型。

```yaml
draft_plan_id: draftplan_real_20260710_001
draft_id: rundraft_20260710_001
status: needs_review
workflow_snapshot_draft_hash: sha256:example
resolved_at: 2026-07-10T10:01:00+08:00
valid_until: 2026-07-10T11:01:00+08:00
core_plan:
  plan_id: dryrun_content-production-real-v0_001
  workflow_id: content-production-real-v0
  valid: false
  estimated_cost:
    min: 14
    max: 42
    currency: CNY
  risks:
    - severity: P0
      code: optional_video_branch_missing_credential
      message: 可选视频分支缺少 VOLC_TTS_API_KEY
      recovery_actions: [configure_credential, switch_provider, skip_optional_branch]
    - severity: P1
      code: manual_gate_required
      message: 工作流包含内容母稿和最终渲染人工审核门
      recovery_actions: [prepare_reviewer, confirm_gate_policy]
  nodes:
    - node_id: A_source_collection
      status: ready
    - node_id: E_tts_caption
      status: blocked
execution_summary:
  node_count: 7
  required_path_count: 1
  optional_branch_count: 1
  execution_policy: hybrid
credential_checks:
  - credential_ref: VOLC_TTS_API_KEY
    required_by: [E_tts_caption]
    required_for_branch: video_package
    status: missing
    blocking_scope: optional_branch
gate_plan:
  - gate_spec_id: gate_content_master
    target: C_md_master
    mode: manual
    required_before: D_script_split
  - gate_spec_id: gate_final_render
    target: F_final_render
    mode: manual
    required_before: G_distribution_retro
branch_impact:
  - branch_id: video_package
    selection: optional
    readiness: blocked
    recovery_actions: [configure_credential, switch_provider, skip_optional_branch]
  - branch_id: markdown_distribution
    selection: required
    readiness: ready
    unaffected_by: [VOLC_TTS_API_KEY]
provider_resolution:
  status: provisional
  nodes:
    - node_id: C_md_master
      candidates: [codex-adapter, official-api-adapter]
      selected: null
provider_cost_estimate:
  available: false
  currency: CNY
  min: null
  max: null
  confidence: unavailable_until_adapter_selection
duration_estimate:
  min_seconds: null
  max_seconds: null
  confidence: unavailable_until_adapter_selection
risks:
  - severity: P0
    code: final_human_review_required
    blocking_scope: run_completion
  - severity: P1
    code: optional_video_branch_missing_credential
    blocking_scope: video_package
startability:
  required_path: ready
  full_workflow: blocked
  recommended_action: start_without_optional_video_branch
plan_hash: sha256:example
```

### 5.1 Dry-run 必须回答的问题

| 问题 | 输出位置 |
|---|---|
| 哪些节点会执行？ | `execution_summary`、节点计划。 |
| 哪些分支是必选或可选？ | `branch_impact`。 |
| 缺少什么凭证？ | `credential_checks`。 |
| 缺凭证会阻断哪里？ | `blocking_scope`、`unaffected_by`。 |
| 哪些位置需要人工审核？ | `gate_plan`。 |
| 会使用哪个 Agent/Adapter/Provider？ | `provider_resolution`。 |
| 成本和时长能否可靠估计？ | estimate 与 confidence。 |
| 当前是否允许启动？ | `startability`。 |

`core_plan.estimated_cost` 是现有核心契约的节点数量启发式估算；
`provider_cost_estimate` 才是 Adapter/Provider 报价。尚未选择真实 Adapter 时，后者不得
伪造数值，必须返回 `available: false`、空值和原因。

## 6. 启动确认门

启动确认使用独立的 `LaunchConfirmation`，避免与运行中的审核 Gate 混淆。

```yaml
confirmation_id: launch_confirm_001
draft_id: rundraft_20260710_001
draft_plan_id: draftplan_real_20260710_001
plan_hash: sha256:example
decision: pending
required_acknowledgements:
  - final_human_review_required
  - optional_video_branch_skipped
  - no_real_adapter_selected
actor: null
comment: null
decided_at: null
```

用户可执行：

- `confirm`：确认当前 plan hash 和风险摘要。
- `revise`：返回草案修改输入、分支或 Provider 候选。
- `cancel`：取消草案，不创建正式 Run。

确认必须绑定 `plan_hash`。任何影响节点、分支、凭证、审核或 Provider 的变化都会生成新
plan hash，旧确认不可复用。

## 7. API 草案

| API | 用途 | 是否调用 Runner |
|---|---|---:|
| `POST /api/v0/run-drafts` | 从 WorkflowTemplate 创建 RunDraft。 | 否 |
| `GET /api/v0/run-drafts/:draftId` | 读取草案、计划和确认状态。 | 否 |
| `PATCH /api/v0/run-drafts/:draftId` | 修改输入、分支和执行策略。 | 否 |
| `POST /api/v0/run-drafts/:draftId/dry-run` | 生成 RunDraftDryRunPlan，并内嵌核心 DryRunPlan。 | 否 |
| `POST /api/v0/run-drafts/:draftId/confirmation` | confirm/revise/cancel。 | 否 |
| `POST /api/v0/runs` | 复用既有启动入口，从已确认草案创建正式 RunSpec。 | P5-08 前禁用 |

`POST /api/v0/runs` 的草案启动请求：

```json
{
  "draft_id": "rundraft_20260710_001",
  "draft_plan_id": "draftplan_real_20260710_001",
  "plan_hash": "sha256:example",
  "confirmation_id": "launch_confirm_001",
  "execution_policy": "hybrid",
  "idempotency_key": "launch-rundraft_20260710_001-sha256-example"
}
```

该入口必须复用 P3 已定义的 `startRun` 应用服务，原子创建 `RunSpec`、
`WorkflowSnapshot`、初始 `NodeRun` 和 `run_created`。Run draft API 不能拥有第二套创建
Run 的事务或事件写入逻辑。

启动前置条件：

1. 草案状态为 `confirmed`。
2. confirmation 的 `plan_hash` 与最新 RunDraftDryRunPlan 一致。
3. Workflow source hash 未变化。
4. required path 的凭证、Gate、组件和 Provider 都可解析。
5. P5-08 已注册可用 Adapter，且用户具有启动权限。

P5-07 调用 `POST /api/v0/runs` 必须返回 `409 adapter_not_ready`，且草案继续保持
`confirmed`，不能创建半成品 RunSpec。只有启动命令通过全部前置检查、获得唯一
`operation_id` 后，草案才能进入 `launch_pending`。相同 `idempotency_key` 的重试返回同一
启动结果；事务失败时状态回滚为 `confirmed`。

## 8. UI 操作流

```text
新任务
-> 选择 content-production-real-v0
-> 填写主题与输入
-> 选择必选/可选分支
-> 创建 Run 草案
-> Dry-run
-> 查看凭证、Gate、Provider、成本、时长和分支影响
-> 修改草案并重新 Dry-run（可选）
-> 人工确认
-> 等待 P5-08 Adapter 启动能力
```

页面必须显式显示模式：

| 模式 | 标签 | 可执行动作 |
|---|---|---|
| 历史导入 | `Historical · Read-only` | 查看、筛选、审计。 |
| 新 Run 草案 | `Run Draft · Not started` | 编辑、Dry-run、确认、取消。 |
| 正式 Run | `Run · running/paused/...` | 执行、审核、恢复、审计。 |

草案页不能显示“运行中”“已完成节点”或正式事件时间线。允许显示的是启动准备日志，例如
`draft_created`、`dry_run_generated`、`launch_confirmed`，这些记录存放在草案审计文件中，
不属于 `TraceEvent`。

## 9. 本地文件布局

```text
.miracle/
  run-drafts/
    rundraft_20260710_001/
      run_draft.json
      workflow_snapshot_draft.json
      run_draft_dry_run_plan.json
      launch_confirmation.json
      draft_audit.jsonl
```

草案目录不放入 `.miracle/runs/`。转换成功后创建新的 run 目录并在草案中记录
`converted_run_id`；不得移动或覆盖原草案文件，以便审计启动前决策。

## 10. 审计规则

草案审计允许的事件类型：

- `run_draft_created`
- `run_draft_updated`
- `dry_run_started`
- `dry_run_generated`
- `dry_run_invalidated`
- `launch_confirmation_recorded`
- `run_draft_cancelled`
- `run_draft_expired`
- `run_draft_converted`

每条记录包含 `draft_id`、`actor`、`timestamp`、`previous_hash`、`next_hash`、
`changed_fields` 和 `correlation_id`。草案审计由 Sidecar 单写入；Web、Agent 和 Adapter
只能提交命令，不能直接追加文件。

## 11. 验收场景

### 11.1 可选视频分支缺凭证

- TTS 凭证缺失。
- Dry-run 将 `video_package` 标为 blocked。
- `markdown_distribution` 保持 ready。
- 用户确认跳过可选视频分支后，草案可进入 `ready_for_confirmation`。

### 11.2 最终人工审核不可跳过

- F 阶段 Gate 在 Dry-run 中必须出现。
- 用户不能通过 Run 草案关闭该 Gate。
- 启动确认只确认风险，不等同于 F 阶段 GateDecision。

### 11.3 草案变化使确认失效

- 用户确认后修改主题、分支或 Provider 候选。
- 系统生成新 plan hash。
- 旧 LaunchConfirmation 变为 `superseded`，必须重新确认。

### 11.4 不产生虚假执行事实

- 创建、修改、Dry-run 和确认草案后，`.miracle/runs/` 不新增目录。
- 不新增 `RunSpec`、`NodeRun`、`NodeAttempt` 或正式 `TraceEvent`。
- `POST /api/v0/runs` 在 P5-08 前返回明确的未就绪错误，并保持草案为 `confirmed`。

## 12. P5-08 交接

P5-08 必须基于本文件评估：

1. Codex Adapter 与官方 API Adapter 哪个适合作为首个真实执行入口。
2. Adapter 注册、能力发现、凭证检查和 Provider receipt 契约。
3. 既有 `POST /api/v0/runs` 如何复用统一 `startRun` 服务，原子创建 RunSpec、
   WorkflowSnapshot、初始 NodeRun 和 `run_created`。
4. Adapter 未启动、超时、取消和崩溃时的审计与回滚边界。
5. 如何保证 Flow A-G 只是样本，Adapter 契约仍面向通用 capability。

## 13. P5-07 验收结论

| 验收项 | 结果 |
|---|---|
| RunDraft 与 RunSpec 边界明确 | 通过 |
| WorkflowSnapshotDraft 与正式快照边界明确 | 通过 |
| Dry-run 凭证、Gate、分支和风险输出明确 | 通过 |
| 启动确认与运行 Gate 不混用 | 通过 |
| 不调用真实 Runner、不写正式运行事实 | 通过 |
| 历史只读、草案、正式 Run 的 UI 模式可区分 | 通过 |
| P5-08 Adapter 交接条件明确 | 通过 |

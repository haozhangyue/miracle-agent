# 15-4_架构方案第四次评审意见

> 文档状态：历史评审记录。第四次评审中的 4 项 P1 与 3 项 P2 已按 MVP 边界完成
> 修订；未引入 NodeOperation 持久化对象、消息队列、分布式事务或完整数据库设计。
> 当前有效规则以 `01`、`03`、`09`、`10`、`12`、`14` 为准。

## 1. 评审背景

本文是第三次架构评审意见完成修订后的第四轮全项目审核。

本轮已经确认以下核心模型完成收口：

- `NodeSpec -> NodeRun -> NodeAttempt` 分层成立。
- NodeRun 是同一次 run 中固定节点的聚合实例。
- retry、fallback、返工和重新执行通过 NodeAttempt 保留执行历史。
- RunSpec 已冻结 WorkflowSnapshot、组件版本和 ProviderPolicy。
- ArtifactSpec 与 ArtifactManifest 已分层。
- 产物路径已支持 artifact version 或 attempt ID。
- GateInstance 已绑定具体 ArtifactManifest。
- GateDecision 已绑定 artifact ID 和 hash。
- NodeAttempt 创建前已冻结 resolved input artifact ID 和 hash。
- Event Journal 已成为权威运行源，其他运行对象作为可重建投影。
- RunSpec 已增加主状态和 attention flags。
- 历史评审文档已明确标记为历史记录。
- Flow A-G 已补齐 nodes、edges、gates、artifacts、provider policy 和 layouts。
- Mermaid 与 PNG 架构图已经同步。

第四轮没有发现需要推倒重做的 P0 问题。当前剩余问题集中在外部副作用调用窗口、
operation ID 生命周期、可选分支依赖、Journal 重建数据完整性及少量状态定义。

## 2. 总体结论

建议在进入 `16_产品信息架构与设计图规划.md` 前处理 4 项 P1。

3 项 P2 不会推翻当前架构，可以与产品信息架构或 P3 技术详细设计同步修订。

| 优先级 | 问题 | 主要风险 |
|---|---|---|
| P1 | 外部调用成功到 Journal 落盘之间存在窗口 | 崩溃后可能重复发布或重复付费 |
| P1 | operation ID 在返工和主动重跑时边界不清 | 新业务结果可能被旧成功记录错误拦截 |
| P1 | G 节点可选视频输入与固定 F -> G 依赖冲突 | 纯 Markdown 流程可能被视频分支阻塞 |
| P1 | received 事件没有保存完整 AdapterResult | 投影丢失后无法从 Journal 完整重建 |
| P2 | TraceEvent 强制要求 node ID | Run、Gate、Registry 级事件表达不自然 |
| P2 | GateInstance 与 GateDecision 状态重复 | blocked/skipped 的归属和恢复规则不清 |
| P2 | default_release_status 未正式定义 | 无审核门产物何时可进入下游不明确 |

## 3. P1-1 外部调用前必须先记录 dispatched

### 3.1 当前问题

当前调用顺序为：

```text
Orchestrator 调用 Adapter
-> Adapter 调用外部 Runtime
-> Runtime 返回结果
-> Orchestrator 写 adapter_result_received
```

`adapter_result_received` 可以解决“结果已经收到，但投影只写了一半”的问题，却无法
解决“外部平台已经执行成功，但结果尚未来得及写入 Journal”的问题。

### 3.2 具体场景：小红书已发布但本地没有记录

G 节点执行发布：

1. Orchestrator 调用小红书 Adapter。
2. 小红书成功创建帖子。
3. Adapter 收到平台成功响应。
4. Orchestrator 在写 `adapter_result_received` 前崩溃。

重启后 Journal 中没有：

```text
adapter_result_received
provider_receipt
发布成功记录
```

系统只能看到该 NodeRun 尚未完成。如果直接重试，就可能发布两篇相同内容。

### 3.3 建议提交协议

在真实外部调用前追加 dispatched 事件：

```text
resolve inputs
-> create NodeAttempt
-> append attempt_dispatched
-> call provider
-> append adapter_result_received
-> project Attempt / Manifest / NodeRun
-> append adapter_result_committed
```

事件示例：

```yaml
event_type: attempt_dispatched
run_id: run_001
node_run_id: run_001_G_publish
attempt_id: publish_attempt_1
operation_id: publish_run001_xiaohongshu_v1
payload:
  adapter: xiaohongshu
  execution_mode: real-run
  resolved_inputs:
    distribution_pack:
      artifact_id: distribution_pack_v1
      hash: sha256:pack111
```

### 3.4 恢复规则

| Journal 状态 | 恢复动作 |
|---|---|
| 没有 dispatched | 尚未调用 provider，可以正常调度 |
| 有 dispatched，无 received | 进入 reconcile，不直接重试 |
| 有 received，无 committed | 不调用 provider，只补齐投影 |
| 有 committed | 本次 Attempt 已完成提交 |

### 3.5 Provider 幂等键

如果外部平台支持幂等键，应将稳定的 `operation_id` 传给平台：

```yaml
provider_request:
  idempotency_key: publish_run001_xiaohongshu_v1
```

如果平台不支持幂等键，应使用：

- 发布内容指纹。
- 本地 operation ID。
- 平台查询接口。
- 人工确认。

进行 reconcile。

### 3.6 修改范围

- `09_WorkflowSpec与Registry技术草案.md`
- `12_MVP原型功能清单与界面草图.md`
- `14_技术架构选型与系统架构图.md`
- 相关 Mermaid 和 PNG 时序图。

## 4. P1-2 operation ID 需要区分重试与新业务操作

### 4.1 当前问题

当前规则为：

```text
operation_id 是稳定业务操作 ID，重试时不变。
attempt_id 每次执行变化。
```

这个规则适用于网络重试和 provider fallback，但审核返工、输入变化和用户主动重跑
通常已经是新的业务操作。

### 4.2 具体场景：母稿返工被旧成功结果拦截

第一次生成 MD 母稿：

```yaml
operation_id: generate_md_run001
attempt_id: generate_md_attempt_1
status: succeeded
artifact_id: md_master_v1
```

用户审核后要求：

```text
重新调整标题和第二段内容。
```

系统准备生成 v2。如果仍使用：

```yaml
operation_id: generate_md_run001
```

执行前检查可能发现该 operation 已经成功，从而直接复用 v1，无法生成返工版本。

### 4.3 具体场景：同一次外部调用的网络重试

调用 TTS 时网络临时失败：

```yaml
operation_id: tts_run001_storyboard_v2
attempt_id: tts_attempt_1
status: failed
```

第二次只是重试相同输入和相同业务意图：

```yaml
operation_id: tts_run001_storyboard_v2
attempt_id: tts_attempt_2
status: succeeded
```

这里 operation ID 应保持不变。

### 4.4 建议 operation 生命周期

| 场景 | operation ID | attempt ID |
|---|---|---|
| 网络重试 | 不变 | 新建 |
| provider fallback | 不变 | 新建 |
| unknown 核对后确认未执行，再重试 | 不变 | 新建 |
| 审核驳回后返工 | 新建 | 新建 |
| 用户主动重新执行已完成节点 | 新建 | 新建 |
| resolved input artifact 发生变化 | 新建 | 新建 |
| execution mode 从 preview 变为 real-run | 新建 | 新建 |

### 4.5 建议引入 NodeOperation

如果希望对象关系更加明确，可以增加轻量聚合：

```text
NodeRun
└── NodeOperation
    ├── NodeAttempt 1
    └── NodeAttempt 2
```

NodeOperation 示例：

```yaml
operation_id: generate_md_run001_v2
node_run_id: run001_B_md_master
reason: review_rework
input_fingerprint: sha256:input222
execution_mode: real-run
created_at: 2026-06-18T12:00:00+08:00
```

如果 MVP 不希望新增持久化对象，也至少应在协议中定义：

```text
operation_id = node_run_id + business_revision + input_fingerprint + execution_mode
```

### 4.6 修改范围

- `01_核心架构与对象模型.md`
- `03_智能路由与工作流编排设计.md`
- `09_WorkflowSpec与Registry技术草案.md`
- `14_技术架构选型与系统架构图.md`

## 5. P1-3 Flow A-G 的可选视频分支需要明确 join 语义

### 5.1 当前问题

G 分发复盘节点将 final_video 声明为可选输入：

```yaml
- name: approved_final_video
  artifact_spec_id: final_video
  required: false
```

但 DAG 中存在固定依赖：

```yaml
- from: F_final_render
  to: G_distribution_retro
  passes_artifacts: [final_video]
```

同时，G 必需的 `md_master` 没有对应的 `B -> G` artifact edge。

### 5.2 具体场景：只做 Markdown 不做视频

用户选择：

```text
本次只生成 MD 母稿和分发包，不制作视频。
```

执行结果：

- B 已生成并批准 md_master。
- C、D、E、F 视频分支被跳过。
- G 的 final_video 是 optional。

但如果执行依赖只看固定 edges，G 仍然有 F 这个上游节点，可能一直等待 F 完成，
导致纯 Markdown 流程无法进入分发复盘。

### 5.3 具体场景：G 找到 md_master，但图上没有来源边

G 的 selector 可以全局查到当前 run 的 md_master，但 EdgeSpec 又被定义为唯一执行依赖
来源。如果没有 `B -> G`，则出现两套依赖系统：

- selector 认为输入存在。
- DAG 认为 B 与 G 没有依赖关系。

这会影响可视化、调度和 validate。

### 5.4 建议 Flow A-G 调整

增加必需边：

```yaml
- id: edge_B_G
  from: B_md_master
  to: G_distribution_retro
  passes_artifacts: [md_master]
  required: true
```

将视频边标记为可选：

```yaml
- id: edge_F_G
  from: F_final_render
  to: G_distribution_retro
  passes_artifacts: [final_video]
  required: false
```

NodeSpec 定义 join policy：

```yaml
join_policy:
  start_when: required_inputs_ready
  optional_inputs:
    wait_policy: no_wait
```

### 5.5 可选输入的其他策略

| 策略 | 行为 |
|---|---|
| `no_wait` | 必需输入满足后立即启动，不等待可选输入 |
| `wait_if_active` | 可选分支已启动时等待，否则不等待 |
| `wait_until_timeout` | 最多等待指定时间 |
| `manual` | 由用户决定是否等待可选分支 |

Flow A-G 推荐：

```text
如果视频分支未启用：no_wait。
如果视频分支已启动：wait_if_active。
```

### 5.6 Validate 规则

- required input 必须存在 required artifact edge。
- optional input 可以通过 optional edge 提供。
- selector 默认只能选择可达 edge 所传递的 ArtifactSpec。
- 如果允许 run-wide 查询，必须显式声明 `source_scope: run`。
- required edge 被 skipped 时，必须有 skip propagation 或替代输入规则。

### 5.7 修改范围

- `03_智能路由与工作流编排设计.md`
- `09_WorkflowSpec与Registry技术草案.md`
- `12_MVP原型功能清单与界面草图.md`

## 6. P1-4 Journal 必须保存完整可重建的 AdapterResult

### 6.1 当前问题

Event Journal 被定义为权威源，但当前 `adapter_result_received` 示例只保存：

```yaml
status: succeeded
provider_receipt: volc_request_92837
```

它没有保存：

- 产物路径。
- 产物 hash。
- ArtifactSpec ID。
- failure metadata。
- provider/runtime metadata。
- 成本和耗时。
- Adapter 返回的标准事件。

如果 ArtifactManifest、NodeAttempt 或其他投影损坏，仅凭该 received 事件无法重建。

### 6.2 具体场景：Manifest 文件损坏

TTS 已执行成功，Journal 中存在：

```yaml
event_type: adapter_result_received
payload:
  status: succeeded
  provider_receipt: volc_request_92837
```

但 ArtifactManifest 文件被误删。重放 Journal 时，系统不知道：

- 音频文件路径。
- 音频 hash。
- 产物属于哪个 ArtifactSpec。
- 是否还有 captions 目录。

因此无法恢复 Artifact Board 和下游输入。

### 6.3 建议 received 事件保存完整脱敏结果

```yaml
event_type: adapter_result_received
operation_id: tts_run001_storyboard_v2
attempt_id: tts_attempt_2
payload:
  status: succeeded
  adapter_id: official_api
  provider: volc_tts
  provider_receipt: volc_request_92837
  duration_ms: 80320
  cost:
    currency: CNY
    amount: 0.42
  artifacts:
    - artifact_id: audio_run001_v2
      artifact_spec_id: audio_manifest
      path: runs/run001/06_音频字幕/tts_attempt_2/audio_manifest.json
      hash: sha256:audio222
    - artifact_id: captions_run001_v2
      artifact_spec_id: captions
      path: runs/run001/06_音频字幕/tts_attempt_2/captions
      hash: sha256:captions222
  failure: null
  runtime_events:
    - event_type: provider_call_completed
```

真实凭证、token、cookie 和私密输入仍然必须脱敏。

### 6.4 投影幂等规则

投影必须使用稳定键进行 upsert：

| 投影 | 稳定键 |
|---|---|
| NodeAttempt | attempt_id |
| NodeRun | node_run_id |
| ArtifactManifest | artifact_id |
| GateInstance | gate_instance_id |
| GateDecision | decision_id |
| ReconciliationRecord | reconciliation_id |

重复重放同一个 received 事件时：

- 不能生成第二个 ArtifactManifest。
- 不能增加 attempt_count。
- 不能重复创建 GateInstance。
- 已存在且内容一致时视为幂等成功。
- 稳定 ID 相同但内容冲突时应进入 projection conflict。

### 6.5 committed 的职责

`adapter_result_committed` 只表示：

```text
该 received 事件对应的所有投影已经成功生成。
```

它不应成为唯一保存 artifact ID 或业务结果的位置，否则 received 仍然不能单独恢复。

### 6.6 修改范围

- `09_WorkflowSpec与Registry技术草案.md`
- `12_MVP原型功能清单与界面草图.md`
- `14_技术架构选型与系统架构图.md`

## 7. P2-1 TraceEvent 应使用通用 subject

### 7.1 当前问题

当前统一事件 envelope 强制要求：

```yaml
run_id:
node_id:
event_type:
```

但部分事件并不属于某个节点。

### 7.2 具体场景

以下事件没有自然的 node ID：

- run_created
- run_cancel_requested
- run_completed
- workflow_migrated
- registry_template_published
- credential_check_completed
- local_service_security_violation

如果强行填写 node ID，只能使用 null、system 或虚构节点。

### 7.3 建议事件 envelope

```yaml
event_id: evt_000001
sequence: 1
run_id: run_001
event_type: run_created
event_category: runtime
subject:
  type: run
  id: run_001
actor:
  type: user
  id: local_user
timestamp: 2026-06-18T10:00:00+08:00
payload: {}
```

subject 类型可以包括：

```text
run / node_run / node_attempt / artifact / gate_instance /
credential / workflow / registry / local_service
```

`run_id` 在 run 外事件中也可以为空，例如 Registry 模板发布。

## 8. P2-2 GateInstance 只保存过程状态

### 8.1 当前问题

当前状态定义为：

```text
GateInstance:
pending_review / decided / blocked / skipped / invalidated

GateDecision:
approved / auto-approved / rejected / blocked / skipped
```

`blocked` 和 `skipped` 同时存在于 Instance 和 Decision，容易出现两套事实。

### 8.2 具体场景：审核被阻塞

最终视频等待人工审核，但本地文件暂时不可访问。

如果写成：

```yaml
gate_instance:
  status: blocked

gate_decision:
  decision: blocked
```

系统无法判断：

- blocked 是审核过程暂时无法继续。
- 还是审核人已经做出了最终 block 决定。
- 文件恢复后应继续当前 GateInstance，还是创建新的 GateInstance。

### 8.3 建议状态职责

GateInstance：

```text
pending_review / decided / invalidated
```

GateDecision：

```text
approved / auto-approved / rejected / blocked / skipped
```

规则：

- `pending_review` 表示等待最终决定。
- 最终 Decision 创建后，Instance 进入 `decided`。
- artifact hash 变化或绑定失效时，Instance 进入 `invalidated`。
- 暂时无法读取文件属于 attention/blocking reason，不是 GateInstance 终态。
- blocked 条件修复后如需重新决策，创建新的 GateInstance，保留旧 Decision。

## 9. P2-3 无审核门产物的默认发布状态需要正式定义

### 9.1 当前问题

Flow A-G 正式样例使用：

```yaml
review_required: false
default_release_status: approved
```

但 ArtifactSpec v0 尚未正式定义 `default_release_status` 的合法值和生效时机。

### 9.2 具体场景：clean_events 是否自动批准

A 节点成功生成 clean_events：

```yaml
review_required: false
default_release_status: approved
```

需要明确：

- NodeAttempt succeeded 后是否立即创建 approved Manifest。
- 是否还需要自动校验 schema、来源和文件存在性。
- 如果 validator 失败，Manifest 是 draft、rejected 还是 blocked。

### 9.3 建议模型

```yaml
artifact_spec:
  id: clean_events
  review_policy:
    mode: auto
    validators:
      - file_exists
      - schema_valid
      - sources_present
    on_pass: approved
    on_fail: rejected
```

审核策略可定义为：

```text
none / auto / manual / conditional
```

推荐规则：

- `none`：Attempt succeeded 后进入 approved。
- `auto`：validators 通过后进入 approved。
- `manual`：进入 pending_review 并创建 GateInstance。
- `conditional`：根据风险规则选择 auto 或 manual。

相比单独使用 `default_release_status`，`review_policy` 能明确状态转换原因。

## 10. 已通过项

本轮以下事项通过评审，不建议继续反复调整：

1. NodeSpec、NodeRun、NodeAttempt 三层关系。
2. NodeRun 固定聚合、NodeAttempt 保留不可变历史。
3. RunSpec snapshot 和 resolved policy 冻结。
4. ArtifactSpec 与 ArtifactManifest 分层。
5. ArtifactManifest 记录 attempt、version、path 和 hash。
6. GateInstance 绑定具体 artifact ID。
7. GateDecision 绑定 artifact ID 和 hash。
8. 文件 hash 变化后旧审核结果失效。
9. NodeAttempt 执行前冻结 resolved inputs。
10. Event Journal 为权威源、运行对象为可重建投影。
11. received/committed 处理投影写入中断。
12. Run 主状态与 attention flags。
13. 历史评审文档时效说明。
14. Flow A-G 完整 YAML 和引用关系。
15. 架构 PNG 已同步最新对象模型。

## 11. Flow A-G 验证结果

本轮对 `09_WorkflowSpec与Registry技术草案.md` 中的正式 Flow A-G YAML 做了实际解析：

```text
YAML 解析成功。
Node：8
Edge：7
Gate：4
Artifact：12
```

并检查了：

- Node input/output ArtifactSpec 引用。
- Node review gate 引用。
- Edge from/to 和 passes_artifacts 引用。
- Artifact producer/consumer 引用。
- Gate node/reject_to/required_before 引用。

引用完整性检查通过。

该验证只证明当前文档样例语法和引用关系成立，不等同于完整 JSON Schema 和真实 runner
测试。

## 12. 建议修改顺序

### 第一阶段：进入产品 IA 前

1. 增加 `attempt_dispatched` 和 dispatched/received/committed 恢复规则。
2. 定义 operation ID 在 retry、返工、重跑和输入变化时的生命周期。
3. 修正 Flow A-G 的 B -> G 必需边、F -> G 可选边及 join policy。
4. 让 received 事件保存完整脱敏 AdapterResult，并定义投影 upsert 规则。

### 第二阶段：可与产品 IA/P3 同步

5. TraceEvent 改为通用 subject。
6. 收口 GateInstance/GateDecision 状态职责。
7. 用 review_policy 正式替代或定义 default_release_status。

## 13. 第四次修订验收标准

完成本轮修订后，应能清楚回答：

1. 外部平台已执行但 received 尚未写入时，重启如何避免重复调用？
2. 哪些 NodeAttempt 共用 operation ID，哪些场景必须创建新 operation？
3. 审核返工为什么不会被上一次成功 operation 拦截？
4. 纯 Markdown 模式下 G 节点为什么不需要等待视频分支？
5. G 的 md_master 输入在 DAG 中通过哪条边到达？
6. 可选分支已经启动时，G 是否等待以及等待多久？
7. Manifest 投影丢失后，received 事件是否包含完整重建数据？
8. 同一 received 事件重放两次是否会生成重复 ArtifactManifest？
9. Run 级事件如何表达而不虚构 node ID？
10. Gate blocked 是过程状态还是最终决定？
11. 无人工审核的产物在什么条件下进入 approved？

## 14. 最终建议

第四轮评审结论：

```text
当前整体架构已经稳定，没有 P0 级问题。
第三次评审提出的核心模型均已正确落地。
剩余 P1 主要解决真实外部执行的最后几个正确性窗口，
完成后即可进入产品信息架构与原型规划。
```

本轮不建议继续扩展对象数量或提前设计完整数据库。重点是把现有协议的边界条件定义
完整，确保后续实现不会出现重复副作用、输入漂移或不可恢复的投影状态。

## 15. 修订实施结果

本轮最终采用“修协议、不扩对象”的处理：

- 接受 4 项 P1 和 3 项 P2。
- 增加 `attempt_dispatched -> adapter_result_received -> projections ->
  adapter_result_committed` 恢复协议。
- operation 使用 business revision、input fingerprint 和 execution mode 派生，不新增
  NodeOperation 持久化对象。
- Flow A-G 增加 B -> G required edge，将 F -> G 改为 optional edge；正式样例现为
  8 nodes、8 edges、4 gates、12 artifacts。
- received 保存完整脱敏 AdapterResult，投影按稳定键 upsert。
- TraceEvent 改用通用 subject；GateInstance 收口为三个过程状态。
- ArtifactSpec 使用 `review_policy: none / auto / manual / conditional`。
- 5 张架构 PNG 已同步上述协议。

未采用的过度设计：

- 不引入消息队列。
- 不引入分布式事务。
- 不引入完整事件溯源框架。
- 不提前确定数据库表结构。

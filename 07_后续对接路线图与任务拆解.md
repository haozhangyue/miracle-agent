# 07_后续对接路线图与任务拆解

## 1. 总体路线

Miracle 后续建议按 6 个阶段推进：

```text
P0 架构评审
-> P1 方案技术设计
-> P2 原型设计
-> P3 技术详细设计
-> P4 MVP 实现
-> P5 真实工作流接入与进化
```

第一版当前完成的是 P0 前的总体规划文档。

## 2. P0 架构评审

目标：确认 Miracle 的系统边界、核心对象、双视图编排、多 Agent 可视化和多平台 adapter 方向。

输入：

- 本目录 00-07 文档。
- 现有“热点工具更新”项目文档。
- 现有 Flow A-G 真实交付包。

需要评审的问题：

| 问题 | 决策 |
|---|---|
| Miracle 第一版是否坚持本地优先？ | 建议本地优先。 |
| Runtime Adapter 是否多平台同步设计？ | 是，Codex/Hermes/OpenClaw/API 同构。 |
| 双模式编排是否进入 MVP？ | 是，至少做可点击原型。 |
| 组件库是否作为核心抽象？ | 是，避免全部节点化带来的使用成本。 |
| 是否先接入热点工具更新？ | 是，作为第一个真实样本。 |

输出：

- 架构评审纪要。
- 关键取舍记录。
- MVP 范围确认。

## 3. P1 方案技术设计

目标：把规划文档转成可实现的技术方案，但仍不急着写业务代码。

需要产出：

1. 数据模型草案。
2. WorkflowSpec JSON/YAML schema。
3. NodeSpec JSON/YAML schema。
4. ComponentSpec schema。
5. AgentSpec schema。
6. TraceEvent schema。
7. Adapter 接口草案。
8. 权限和密钥边界说明。

重点设计：

- 如何从现有 `task_trace.json` 兼容迁移。
- 如何导入“热点工具更新” Flow A-G。
- 如何表达无限画布和 DAG 的双布局。
- 如何记录 Agent 运行状态。
- 如何处理外部大文件和媒体产物。

## 4. P2 原型设计

目标：先做产品原型，而不是先写完整系统。

必须验证 3 个 UI：

### 4.1 Agent Collaboration View 原型

验证：

- Agent Map。
- Agent 状态卡。
- 当前任务。
- 等待关系。
- 阻塞原因。
- 交接产物。

### 4.2 Workflow Node Builder 原型

验证：

- Flow A-G 节点展示。
- 节点状态。
- 插入 Pencil 节点。
- 子工作流折叠展开。
- 审核门展示。
- failed/blocked 展示。

### 4.3 Infinite Canvas Builder 原型

验证：

- 主题区、事实区、原型区、内容区、视频区、分发区、复盘区。
- 任务卡转节点。
- 产物卡和节点卡联动。
- 画布切换到 DAG。

输出：

- 低保真原型。
- 高保真原型。
- 核心交互说明。
- 原型验收清单。

## 5. P3 技术详细设计

目标：确定 MVP 技术架构和任务拆解。

需要确定：

- 前端框架。
- 画布/节点图方案。
- 本地数据存储。
- 后端或本地服务形态。
- Adapter 插件接口。
- 日志和 trace 存储。
- 与现有项目目录的读写边界。

建议 MVP 技术边界：

- 本地优先。
- 先读写本地 JSON/YAML/Markdown。
- 先接入静态 trace 和手动状态，不直接做复杂后台调度。
- 不做账号系统。
- 不做云同步。
- 不做多租户。

## 6. P4 MVP 实现

原始功能点拆解：

| ID | 功能 | 优先级 |
|---|---|---:|
| M001 | Miracle 项目配置和 schema | P0 |
| M002 | 导入热点工具更新 Flow A-G | P0 |
| M003 | 任务列表和 Run 详情页 | P0 |
| M004 | 流程节点视图 | P0 |
| M005 | Agent 状态视图 | P0 |
| M006 | Artifact Board | P0 |
| M007 | Infinite Canvas 原型 | P1 |
| M008 | 双视图同步 | P1 |
| M009 | 插入 Pencil 节点演示 | P1 |
| M010 | 子工作流演示 | P1 |
| M011 | Component Registry v0 | P1 |
| M012 | Runtime Adapter 接口草案 | P1 |
| M013 | Codex Adapter v0 | P2 |
| M014 | Official API Adapter v0 | P2 |
| M015 | Hermes/OpenClaw Adapter 设计占位 | P2 |
| M016 | Evolution Board v0 | P2 |
| M017 | Workflow Registry v0 | P0 |
| M018 | Dry-run / Validate v0 | P0 |
| M019 | Agent Health Dashboard v0 | P0 |
| M020 | Visual/Spec 双向同步 v0 | P1 |

竞品分析后的 MVP 推荐顺序。为避免和上方原始 M001-M020 编号混淆，新顺序使用 `MVPSxx` 编号：

| 顺序 | ID | 功能 | 原因 |
|---:|---|---|---|
| 1 | MVPS01 | WorkflowSpec YAML v0 | 先让工作流可版本化、可 diff、可进入 Git。 |
| 2 | MVPS02 | Flow A-G Importer | 用热点工具更新验证真实流程导入。 |
| 3 | MVPS03 | Validate / Dry-run | 启动前发现凭证、审核门、provider、成本风险。 |
| 4 | MVPS04 | Node DAG View | 先保证严谨执行链路可见。 |
| 5 | MVPS05 | Agent Collaboration View | 展示 Agent 健康、等待、阻塞、交接。 |
| 6 | MVPS06 | Artifact Board | 管理内容、音频、视频、分发和复盘产物。 |
| 7 | MVPS07 | Gate Review UI | 支持批准、驳回、评论、阻塞和返工。 |
| 8 | MVPS08 | Infinite Canvas Prototype | 用空间方式组织内容、素材、产物、分支。 |
| 9 | MVPS09 | Visual/Spec Sync | 让 UI 和 YAML/JSON 保持一致。 |
| 10 | MVPS10 | Evolution Board v0 | 把失败、返工、重复修改变成进化建议。 |

新增任务验收：

| ID | 验收标准 |
|---|---|
| M017 | 能注册本地项目、本机目录、GitHub 仓库和内置模板；模板状态支持 draft、experimental、stable、deprecated、blocked。 |
| M018 | 能输出执行计划、缺失凭证、人工审核门、provider、fallback、预计风险。 |
| M019 | 能展示 Agent 心跳、活跃度、当前节点、阻塞原因、等待对象和恢复动作。 |
| M020 | UI 新增节点能生成 spec diff；YAML 新增节点能显示到 DAG 和 Canvas；冲突不自动覆盖 stable。 |

## 7. P5 真实工作流接入

第一条接入链路：

```text
热点工具更新 Flow A-G -> Miracle WorkflowSpec -> RunSpec -> Trace View -> Agent View -> Artifact Board
```

接入步骤：

1. 抽取现有 Flow A-G 为 `content-production-v0`。
2. 抽取现有 Agent 职责为 AgentSpec。
3. 抽取项目级 skills 为 ComponentSpec。
4. 抽取 TTS、HyperFrames、Pencil 为 ComponentLibrary。
5. 把 `approval_policy.yaml` 转成 GateSpec。
6. 把 `task_trace.json` 和 `task_events.jsonl` 映射到 TraceEvent。
7. 用一个历史 run 做只读展示。
8. 再用一个新 run 做半自动控制。

## 8. 验收计划

### 8.1 文档验收

- 00-07 文档齐全。
- 每个核心对象有定义。
- 双模式编排有明确同步规则。
- 多 Agent 可视化有字段和视图。
- 后续任务拆解可执行。

### 8.2 原型验收

- 能看到 Flow A-G。
- 能看到多个 Agent 状态。
- 能从节点视图切到画布视图。
- 能在 MD 前插入 Pencil 节点。
- 能展开内容精细策划子工作流。
- 能看到 blocked/failed 原因。

### 8.3 MVP 验收

- 能导入一个历史真实任务。
- 能展示节点状态、Agent 状态、产物和审核。
- 能创建一个新 Run 草案。
- 能生成 trace。
- 能记录进化建议。

## 9. 风险与对策

| 风险 | 对策 |
|---|---|
| 抽象过大，迟迟不能落地 | 先用热点工具更新 Flow A-G 做第一条样本。 |
| 全部节点化导致使用成本高 | 默认组件库 + 推荐模板，高级模式再开放细节。 |
| 双视图数据割裂 | 统一 Workflow Graph，视图只存 layout。 |
| 多平台 adapter 太复杂 | 先定义接口，同步设计，分阶段接入。 |
| Auto 模式不可控 | 高风险节点保留审核门，进化建议需人工批准。 |
| 媒体产物过大 | 本地路径/外部链接记录，不进入核心数据库或 Git。 |
| Agent 状态黑箱 | 强制 TraceEvent 和 AgentRun 记录。 |

## 10. 下一步建议

下一轮建议直接进入 Spec 优先的技术设计和原型规划，建议议程：

1. 评审 `09_WorkflowSpec与Registry技术草案.md`。
2. 确认 `WorkflowSpec YAML v0` 是否足够表达 Flow A-G、Pencil 插入和子工作流。
3. 评审 `10_AgentHealth与多Agent状态机设计.md`。
4. 确认 Agent Health Dashboard 的默认信息密度。
5. 评审 `11_VisualBuilder与Spec双向同步设计.md`。
6. 确认 Visual/Spec 同步和冲突策略。
7. 评审 `12_MVP原型功能清单与界面草图.md`。
8. 输出原型阶段任务计划。

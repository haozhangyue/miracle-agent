# 15-3_架构方案第三次评审意见

## 1. 评审背景

本文是对二次架构评审修订后的第三轮全项目审核。

二次修订已经基本完成以下收口：

- RunSpec 创建时冻结 WorkflowSnapshot、组件版本和 ProviderPolicy。
- ArtifactSpec 与 ArtifactManifest 分层。
- NodeRun、GateDecision、ArtifactManifest 状态分层。
- Runtime Adapter 只返回 AdapterResult，由 Orchestrator 统一持久化。
- Local Service 增加 token、Origin/CSRF、workspace allowlist 和危险操作确认。
- TraceEvent 与 AuditEvent 使用统一事件 envelope。
- NodeRun 增加暂停、取消、超时和异常中止状态。
- CredentialSpec 与 CredentialCheckResult 分层。
- Mermaid 和 PNG 架构图同步更新。

第三轮评审没有发现需要推倒重做的 P0 问题。当前剩余问题主要集中在：

1. 完整 WorkflowSpec 样例是否真的可校验。
2. 固定节点、运行实例和多次执行尝试如何建模。
3. 审核结果如何绑定具体产物版本。
4. 下游如何选择唯一且正确的产物实例。
5. JSONL 多对象持久化如何应对中途崩溃。
6. 产物版本路径、Run 级状态和历史评审文档如何收口。

## 2. 总体结论

推荐在进入 `16_产品信息架构与设计图规划.md` 前，先完成 5 项 P1 协议收口。
3 项 P2 可以与产品信息架构同步修改。

| 优先级 | 问题 | 结论 |
|---|---|---|
| P1 | Flow A-G 样例缺少完整 ArtifactSpec 和 GateSpec | 补成首个正式可校验样例 |
| P1 | NodeRun 与多次执行尝试的层级仍不清晰 | NodeRun 固定复用，新增不可变 NodeAttempt |
| P1 | GateDecision 没有绑定具体产物版本 | 增加 GateInstance，并让 Decision 绑定 artifact ID 和 hash |
| P1 | 下游只按 ArtifactSpec ID 选择输入 | 增加 selector，并在执行计划中解析为具体 artifact ID |
| P1 | Orchestrator 多对象持久化缺少崩溃提交协议 | Event Journal 作为权威源，其他对象作为可重建投影 |
| P2 | 产物路径可能覆盖重试版本 | 路径加入 artifact version 或 attempt ID |
| P2 | 缺少 RunSpec 聚合状态机 | 增加 Run 级状态和推导规则 |
| P2 | 历史评审文档保留旧口径 | 标记为历史记录并声明当前事实源 |

## 3. P1-1 Flow A-G 必须成为完整可校验样例

### 3.1 当前问题

`09_WorkflowSpec与Registry技术草案.md` 的 Flow A-G 示例中，节点引用了：

```text
raw_items
clean_events
md_master
storyboard
audio_manifest
captions
final_video
```

但示例没有给出完整的 `artifacts:` 和 `gates:` 定义。当前 validate 规则也只检查
artifact 是否有 producer，没有明确检查每一个输入、输出和边引用的 ArtifactSpec
是否真实存在。

### 3.2 具体场景：名称写错但校验仍然通过

假设 B 节点真实输出：

```yaml
outputs:
  - artifact_spec_id: md_master
```

C0 节点误写为：

```yaml
inputs:
  - artifact_spec_id: md_mater
    required: true
```

这里少写了一个 `s`。如果 validate 只检查“artifact 是否有 producer”，但没有检查
`md_mater` 是否存在，工作流可能在 dry-run 后甚至运行到 C0 才失败。

用户看到的结果会是：

```text
B 节点已经生成母稿
C0 节点却提示找不到输入
```

这不是运行时问题，而是模板引用错误，本应在 validate 阶段阻止。

### 3.3 具体场景：Gate ID 引用不存在

B 节点声明：

```yaml
review_gate: B_md_master_gate
```

但 `gates:` 中因为重命名写成：

```yaml
id: B_content_gate
```

如果 validate 没有做引用完整性检查，B 节点生成母稿后将无法创建正确的审核门实例。

### 3.4 建议补齐的正式样例

Flow A-G 示例至少应包含：

```yaml
id: content-production-v0
nodes: []
edges: []
gates: []
artifacts: []
provider_policy: {}
layouts: {}
```

ArtifactSpec 示例：

```yaml
artifacts:
  - id: md_master
    kind: markdown
    path_template: runs/{run_id}/03_内容母稿/{artifact_version}/MD母稿.md
    produced_by: B_md_master
    consumed_by: [C0_script_pool, G_distribution_retro]
    review_required: true
    gate_id: B_md_master_gate
```

GateSpec 示例：

```yaml
gates:
  - id: B_md_master_gate
    node: B_md_master
    mode: manual
    decisions: [approve, reject, comment, block]
    required_before: [C0_script_pool]
```

### 3.5 Validate 必须增加的检查

- NodeSpec 输入引用的 ArtifactSpec 必须存在。
- NodeSpec 输出引用的 ArtifactSpec 必须存在。
- ArtifactSpec 的 `produced_by` 必须引用存在节点。
- ArtifactSpec 的 `consumed_by` 必须引用存在节点。
- NodeSpec 的 `review_gate` 必须引用存在 GateSpec。
- GateSpec 的 node、reject_to、required_before 必须引用存在节点。
- EdgeSpec 的 passes_artifacts 必须引用存在 ArtifactSpec。
- required input 必须存在可到达的 producer。
- 同一个 ArtifactSpec 不应被多个节点生产，除非明确声明 merge/version 策略。

### 3.6 修改建议

主要修改：

- `09_WorkflowSpec与Registry技术草案.md`
- `12_MVP原型功能清单与界面草图.md`

Flow A-G 应作为后续 YAML schema 和 validate 实现的第一个正式验收样本。

## 4. P1-2 NodeRun 应固定复用，真实执行拆为 NodeAttempt

### 4.1 需要修正的理解

之前“重试创建新的 NodeRun”容易让人误解为：

```text
每次重试都会在 DAG 中新增一个节点。
```

推荐的正确模型是：

```text
NodeSpec：工作流模板中的固定节点。
NodeRun：某个 run 中该节点的固定聚合实例。
NodeAttempt：一次真实执行尝试，不可变。
```

关系如下：

```text
NodeSpec D_tts_caption
└── NodeRun run_001_D_tts
    ├── NodeAttempt attempt_1
    ├── NodeAttempt attempt_2
    └── NodeAttempt attempt_3
```

因此：

- DAG 中仍然只有一个 TTS 节点。
- 同一次 run 中仍然只有一个 TTS NodeRun。
- 每次 retry、fallback 或重新执行只增加一条 NodeAttempt。

### 4.2 具体场景：TTS 超时后重试

固定 NodeSpec：

```yaml
node_spec:
  id: D_tts_caption
  type: tool
```

固定 NodeRun：

```yaml
node_run:
  node_run_id: run_001_D_tts
  run_id: run_001
  node_id: D_tts_caption
  status: running
  current_attempt_id: attempt_1
  attempt_count: 1
```

第一次调用火山 TTS 超时：

```yaml
node_attempt:
  attempt_id: attempt_1
  node_run_id: run_001_D_tts
  operation_id: tts_run001_D
  provider: volc_tts
  status: timed_out
  started_at: 2026-06-18T10:00:00+08:00
  ended_at: 2026-06-18T10:02:00+08:00
```

系统决定切换备用 provider：

```yaml
node_attempt:
  attempt_id: attempt_2
  node_run_id: run_001_D_tts
  operation_id: tts_run001_D
  provider: backup_tts
  status: succeeded
  artifact_ids:
    - audio_run001_v2
```

最终 NodeRun 聚合为：

```yaml
node_run:
  node_run_id: run_001_D_tts
  status: done
  current_attempt_id: attempt_2
  successful_attempt_id: attempt_2
  attempt_count: 2
```

UI 中只显示一个 TTS 节点，可在详情中展开两次执行历史。

### 4.3 为什么不能覆盖第一次记录

如果第二次成功后直接把 NodeRun 改成：

```yaml
status: done
provider: backup_tts
```

系统将丢失：

- 第一次调用使用了哪个 provider。
- 第一次是否已经计费。
- 为什么触发 fallback。
- 第一次是否产生了临时文件。
- 第一次是否其实已在外部成功，只是响应超时。

这些信息直接影响成本统计、故障分析和重复副作用判断。

### 4.4 具体场景：发布结果不明确

G 节点发布小红书：

```text
平台已经发布成功
Miracle 在接收响应时网络超时
```

第一次 Attempt：

```yaml
attempt_id: publish_attempt_1
operation_id: publish_run001_xiaohongshu
status: unknown
provider_receipt: null
reconciliation_required: true
```

此时不能直接创建第二次真实发布 Attempt。系统应先查询平台或等待用户确认：

```yaml
node_run:
  status: reconciling
  current_attempt_id: publish_attempt_1
```

如果确认第一次已经发布：

```yaml
attempt_id: publish_attempt_1
status: succeeded
provider_receipt: xhs_post_92837
```

如果确认第一次没有发布，才创建：

```yaml
attempt_id: publish_attempt_2
operation_id: publish_run001_xiaohongshu
status: queued
```

### 4.5 暂停时是否创建新 Attempt

需要根据底层执行是否仍可原地恢复判断。

| 场景 | NodeRun | NodeAttempt |
|---|---|---|
| 等待用户补充标题，底层会话仍存在 | 复用 | 复用当前 Attempt |
| 本地渲染进程支持原地暂停和继续 | 复用 | 复用当前 Attempt |
| 进程已经被终止 | 复用 | 原 Attempt aborted，新建 Attempt |
| provider 临时失败后重试 | 复用 | 新建 Attempt |
| 切换 provider 或 fallback | 复用 | 新建 Attempt |
| 审核驳回后返工 | 复用 | 新建 Attempt |
| 用户重新执行已完成节点 | 复用 | 新建 Attempt |

### 4.6 是否会造成大量新增开销

不会造成大量节点或 UI 开销。

假设工作流有 20 个固定节点，其中 5 个节点各重试一次：

```text
NodeSpec：20 条
NodeRun：20 条
NodeAttempt：25 条
```

新增的只是 5 条几 KB 的执行元数据，不是新增 5 个 DAG 节点。

真正占空间的是音频、视频和模型输出。NodeAttempt 可以只记录：

- 开始和结束时间。
- provider、adapter。
- 状态和失败原因。
- operation ID、receipt。
- 关联事件和产物 ID。

### 4.7 建议状态职责

NodeRun 聚合状态：

```text
not_started / queued / running / paused / blocked / reconciling /
cancelled / failed / skipped / done
```

NodeAttempt 终态：

```text
succeeded / failed / timed_out / cancelled / aborted / unknown
```

关键规则：

- NodeRun 可以随调度过程更新。
- NodeAttempt 完成后不可覆盖，只允许追加 reconcile 结果事件。
- retry 创建新 NodeAttempt，不创建新 NodeSpec，也不创建新 NodeRun。
- NodeRun 状态由当前 Attempt、历史 Attempts 和调度策略汇总。

### 4.8 修改建议

主要修改：

- `01_核心架构与对象模型.md`
- `03_智能路由与工作流编排设计.md`
- `09_WorkflowSpec与Registry技术草案.md`
- `10_AgentHealth与多Agent状态机设计.md`
- `12_MVP原型功能清单与界面草图.md`
- `14_技术架构选型与系统架构图.md`

## 5. P1-3 审核必须绑定具体产物版本

### 5.1 当前问题

当前 GateDecision 示例主要包含：

```yaml
gate_id: B_md_master_gate
node_id: B_md_master
decision: approved
reviewer: user
```

它没有明确说明用户批准的是哪一个 ArtifactManifest。

### 5.2 具体场景：v1 批准结果错误放行 v2

第一次生成母稿：

```yaml
artifact_id: md_master_v1
hash: sha256:111
status: pending_review
```

用户批准 v1：

```yaml
gate_id: B_md_master_gate
decision: approved
```

之后用户要求调整标题，系统生成：

```yaml
artifact_id: md_master_v2
hash: sha256:222
status: pending_review
```

如果 GateDecision 只绑定 gate ID，系统可能读取“B Gate 已 approved”，错误地让
`md_master_v2` 进入脚本池。实际上用户从未看过 v2。

### 5.3 建议对象分层

#### GateSpec：模板审核规则

```yaml
gate_spec:
  id: B_md_master_gate
  node: B_md_master
  mode: manual
```

#### GateInstance：某次 run 中的一次审核实例

```yaml
gate_instance:
  gate_instance_id: run001_B_gate_v2
  gate_spec_id: B_md_master_gate
  run_id: run_001
  node_run_id: run_001_B
  artifact_ids:
    - md_master_v2
  status: pending_review
```

#### GateDecision：不可变审核决定

```yaml
gate_decision:
  decision_id: decision_run001_B_v2_approve
  gate_instance_id: run001_B_gate_v2
  decision: approved
  reviewer:
    type: user
    id: local_user
  decided_at: 2026-06-18T11:00:00+08:00
  artifact_bindings:
    - artifact_id: md_master_v2
      hash: sha256:222
  comment: 该版本可以进入脚本池
```

### 5.4 具体场景：审核后文件被人工修改

用户批准：

```text
md_master_v2
hash = sha256:222
```

随后有人直接编辑该文件，hash 变成：

```text
sha256:333
```

即使 artifact ID 不变，系统也必须认为原批准失效：

```yaml
status: pending_review
reason: approved_artifact_hash_changed
```

### 5.5 Comment 不应被当成最终 Decision

评论行为：

```yaml
action: comment
message: 第二段需要补充官方来源
```

它不会形成最终放行结论。建议：

- GateInstance 保存当前状态。
- approve/reject/block/skip 形成不可变 GateDecision。
- comment 记录为 GateComment 或 audit TraceEvent。

### 5.6 修改建议

主要修改：

- `01_核心架构与对象模型.md`
- `09_WorkflowSpec与Registry技术草案.md`
- `10_AgentHealth与多Agent状态机设计.md`
- `12_MVP原型功能清单与界面草图.md`

## 6. P1-4 下游必须选择具体 ArtifactManifest

### 6.1 当前问题

当前输入契约类似：

```yaml
inputs:
  - artifact_spec_id: md_master
    required_status: approved
```

这只能说明“需要一个已批准的 md_master”，不能说明具体使用哪一个实例。

### 6.2 具体场景：同一次 run 存在多个已批准版本

某次任务中产生：

```text
md_master_v1：approved
md_master_v2：rejected
md_master_v3：approved
```

C0 脚本池启动时，如果只按：

```text
artifact_spec_id = md_master
status = approved
```

查询，可能同时找到 v1 和 v3。系统无法确定：

- 使用最新批准版本 v3。
- 使用第一次批准版本 v1。
- 让用户手动选择。
- 合并多个版本。

### 6.3 建议模板选择器

NodeSpec 中声明选择策略：

```yaml
inputs:
  - name: approved_md_master
    artifact_spec_id: md_master
    required: true
    selector:
      run_scope: current
      status: approved
      strategy: latest_approved
```

可选策略：

| 策略 | 用途 |
|---|---|
| `latest_approved` | 选择最新批准实例 |
| `first_approved` | 固定选择首次批准实例 |
| `explicit` | 必须由用户或上游明确指定 artifact ID |
| `all_matching` | 批处理所有符合条件实例 |
| `merge` | 交给合并节点处理多个实例 |

### 6.4 执行前必须固化具体输入

Orchestrator 创建 NodeAttempt 前，把选择器解析为具体 artifact ID：

```yaml
node_attempt:
  attempt_id: C0_attempt_1
  resolved_inputs:
    approved_md_master:
      artifact_id: md_master_v3
      hash: sha256:333
      selected_by: latest_approved
```

这样即使 C0 执行期间又产生 v4，当前 Attempt 仍然使用已经冻结的 v3，不会发生输入漂移。

### 6.5 具体场景：optional final_video

G 分发复盘中 final_video 是可选输入：

```yaml
artifact_spec_id: final_video
required: false
```

如果存在一个 rejected 视频和一个 approved 视频，选择器必须只选择 approved 实例；
如果不存在 approved 实例，应解析为“输入缺省”，而不是把 rejected 视频交给分发节点。

### 6.6 修改建议

主要修改：

- `09_WorkflowSpec与Registry技术草案.md`
- `03_智能路由与工作流编排设计.md`
- `12_MVP原型功能清单与界面草图.md`

## 7. P1-5 Orchestrator 持久化需要崩溃提交协议

### 7.1 当前问题

当前已经明确：

```text
Adapter 不直接落库。
Orchestrator 统一持久化 TraceEvent、ArtifactManifest、NodeRun 和 GateDecision。
```

但“统一持久化”不等于“原子持久化”。如果这些对象分多个 JSON/JSONL 文件写入，
进程仍可能在中途崩溃。

### 7.2 具体场景：TTS 成功但只写了一半

Adapter 返回：

```yaml
status: succeeded
provider_receipt: volc_request_92837
artifact:
  path: audio/final.wav
  hash: sha256:789
```

Orchestrator 执行：

1. 写入 `result_received` TraceEvent。
2. 写入 ArtifactManifest。
3. 更新 NodeAttempt。
4. 更新 NodeRun。
5. 写入 `result_committed`。

如果在第 2 步后崩溃，重启时可能看到：

```text
音频 Manifest 已存在
NodeAttempt 仍是 running
NodeRun 仍是 running
```

如果系统把它当成执行失败并重新调用 TTS，就会重复付费。

### 7.3 建议：Event Journal 作为权威源

推荐当前阶段确定以下原则：

```text
TraceEvent/Event Journal 是权威追加日志。
NodeRun、NodeAttempt、ArtifactManifest、GateInstance 是可重建投影。
```

提交过程：

```text
AdapterResult received
-> append result_received
-> project ArtifactManifest
-> project NodeAttempt
-> project NodeRun
-> append result_committed
```

重启恢复：

```text
扫描 result_received 但没有 result_committed 的 operation
-> 不重新调用 provider
-> 继续完成剩余投影
-> 写 result_committed
```

### 7.4 示例事件

```yaml
event_id: evt_201
event_type: adapter_result_received
operation_id: tts_run001_D
attempt_id: attempt_2
payload:
  status: succeeded
  provider_receipt: volc_request_92837
```

投影完成后：

```yaml
event_id: evt_205
event_type: adapter_result_committed
operation_id: tts_run001_D
attempt_id: attempt_2
payload:
  artifact_ids: [audio_run001_v2]
```

### 7.5 sequence 的生成规则

当前事件要求同一 run 内递增 sequence，还需要明确：

- sequence 由 Orchestrator 单写入器分配。
- 一个 run 同时有多个并行节点时，仍使用 run 级全局 sequence。
- event_id 用于去重，sequence 用于恢复顺序。
- 重放投影时按 sequence 处理。
- 已存在 event_id 的写入必须视为幂等成功。

### 7.6 当前阶段不必实现什么

当前不需要马上选择 Kafka、EventStoreDB 或完整数据库事务。

MVP 可以使用：

```text
每个 run 一个 append-only JSONL journal
+ 单写入器
+ fsync/安全落盘策略
+ result_received/result_committed
+ 可重建投影
```

具体文件锁、fsync、分片和 SQLite 表结构可留到 P3。

### 7.7 修改建议

主要修改：

- `09_WorkflowSpec与Registry技术草案.md`
- `14_技术架构选型与系统架构图.md`
- `12_MVP原型功能清单与界面草图.md`

## 8. P2-1 产物路径必须支持多版本

### 8.1 当前问题

当前 ArtifactSpec 路径模板类似：

```yaml
path_template: runs/{run_id}/03_内容母稿/MD母稿.md
```

但规则同时要求重试不覆盖旧 ArtifactManifest。固定路径会导致新版本覆盖旧文件。

### 8.2 具体场景：母稿返工

第一次生成：

```text
MD母稿.md
hash = 111
```

审核驳回后第二次生成仍写：

```text
MD母稿.md
hash = 222
```

虽然系统可能保存了两个 Manifest，但两个 Manifest 都指向同一个路径，v1 文件内容已经丢失。

### 8.3 建议路径

方案一，按 artifact version：

```yaml
path_template: runs/{run_id}/03_内容母稿/{artifact_version}/MD母稿.md
```

方案二，按 attempt：

```yaml
path_template: runs/{run_id}/03_内容母稿/{attempt_id}/MD母稿.md
```

并维护逻辑指针：

```yaml
artifact_alias:
  artifact_spec_id: md_master
  latest_artifact_id: md_master_v3
  latest_approved_artifact_id: md_master_v3
```

### 8.4 大媒体产物

视频文件更不能静默覆盖：

```text
final_video_attempt_1.mp4
final_video_attempt_2.mp4
```

未批准版本可根据保留策略清理，但不能在审核前被自动覆盖。

## 9. P2-2 RunSpec 需要聚合状态机

### 9.1 当前问题

文档已经定义 NodeRun 和 NodeAttempt 状态，但 RunSpec 主要只有：

```yaml
status: running
```

没有说明整个工作流如何表达等待审核、暂停、部分失败和取消。

### 9.2 具体场景：多个分支同时处于不同状态

某次 run 中：

```text
B 母稿：等待审核
D TTS：blocked，缺少凭证
E 视频：not_started
G 分发：not_started
```

RunSpec 应显示什么？

- `running` 会让用户误以为仍在自动推进。
- `blocked` 会掩盖 B 仍然可以人工审核。
- `pending_review` 会掩盖 D 的凭证阻塞。

### 9.3 建议 Run 状态

```text
created / queued / running / paused / pending_review /
blocked / cancelling / cancelled / failed / completed / aborted
```

### 9.4 建议状态优先级

可采用“主状态 + attention flags”：

```yaml
run:
  status: running
  attention:
    - pending_review
    - blocked
```

或者明确聚合优先级：

```text
aborted
> cancelling/cancelled
> failed
> paused
> blocked
> pending_review
> running
> queued
> completed
```

推荐主状态加 attention flags，因为一个复杂工作流可能同时存在多个需要用户处理的问题。

### 9.5 完成条件

Run 进入 completed 至少要求：

- 所有 required 节点达到 done 或合法 skipped。
- 所有 required GateInstance 已 approved/auto-approved/合法 skipped。
- 所有 required ArtifactManifest 已满足下游和最终交付状态。
- 没有 unresolved reconciliation。
- 没有 cancelling 中的执行进程。

## 10. P2-3 历史评审文档需要声明时效

### 10.1 当前问题

`15_架构方案评审意见.md` 和 `15-2_架构方案二次评审意见.md` 是重要决策过程记录，
但其中保留了当时尚未修订的旧状态口径。

例如首次评审中曾把 `pending_review` 放入 NodeRun。现在有效设计已经改为：

```text
NodeRun 不承载审核状态。
GateInstance/GateDecision 和 ArtifactManifest 承载审核与放行。
```

### 10.2 具体场景：新成员按阅读顺序读到旧口径

新成员从 README 顺序阅读：

1. 先看到当前对象模型。
2. 后面又看到首次评审中的旧 NodeRun 状态。
3. 不确定哪个版本才是当前标准。

如果他按照旧评审设计 UI，可能再次把 `approved` 放回 NodeRun。

### 10.3 建议处理

在历史评审文档顶部增加：

```markdown
> 文档状态：历史评审记录。
> 本文保留当时问题和决策过程，不作为当前对象模型事实源。
> 当前有效口径以 01、03、09、10、14 及最新评审决策为准。
```

README 用途列改为：

```text
历史架构评审与修订过程记录
```

不建议删除或重写历史意见，否则会丢失决策演进过程。

## 11. 建议修改顺序

### 第一阶段：P1 对象和协议

1. 引入 `NodeAttempt`，重新定义 NodeRun 为固定聚合实例。
2. 引入 `GateInstance`，让 GateDecision 绑定具体 artifact ID 和 hash。
3. 为 NodeSpec 输入增加 Artifact selector，并在 NodeAttempt 中固化 resolved inputs。
4. 补齐 Flow A-G 的 artifacts、gates 和完整 validate 规则。
5. 定义 Event Journal 的 result_received/result_committed 恢复协议。

### 第二阶段：P2 聚合和文档

6. 修改 ArtifactSpec 路径模板，支持多版本文件。
7. 增加 RunSpec 聚合状态和 attention flags。
8. 给历史评审文档添加时效说明。

### 第三阶段：同步表达

9. 同步 `01/03/09/10/12/14`。
10. 更新相关 Mermaid 和 PNG。
11. 全项目搜索旧的 NodeRun attempt、GateDecision 和 artifact 输入口径。

## 12. 第三次修订验收标准

完成本轮调整后，应能清楚回答：

1. Flow A-G 中每个 artifact 和 gate 是否都有完整定义并可 validate？
2. 一个固定 DAG 节点重试三次后，NodeSpec、NodeRun、NodeAttempt 分别有多少条？
3. 哪些暂停场景复用当前 Attempt，哪些场景必须新建 Attempt？
4. 发布结果 unknown 时，系统为什么不能直接重试？
5. GateDecision 批准的是哪一个 artifact ID 和 hash？
6. 文件在批准后被修改，原批准是否自动失效？
7. 下游发现多个 approved artifact 时按什么规则选择？
8. NodeAttempt 开始执行前是否已经固化具体输入 artifact ID？
9. AdapterResult 写入一半时，重启后如何恢复而不重复调用 provider？
10. 产物重试后旧文件是否仍可追踪？
11. 一个 run 同时 pending review 和 blocked 时，UI 如何表达？
12. 历史评审记录是否会被误认为当前事实源？

## 13. 最终建议

第三轮评审结论：

```text
二次评审提出的主问题已经基本闭环，当前不需要推倒重做。
下一步重点是补齐运行模型的最后一层：
NodeRun 固定聚合，NodeAttempt 记录每次执行；
GateInstance 管理审核过程，GateDecision 绑定具体产物版本；
Event Journal 保证崩溃后可恢复且不重复调用外部能力。
```

完成 5 项 P1 后，架构已经足以支持进入产品信息架构和原型规划。


# 39_P5-03历史Run只读导入方案

> 任务状态：P5-03 已完成。  
> 输入依据：`37_P5-01真实工作区盘点报告.md`、`38_P5-02FlowAG对象映射设计.md`、W24/W23 真实交付包、现有 MVP run 目录结构。  
> 目标：定义历史 Run 只读 importer 的输入、输出、忽略规则、projection 边界和 W24/W23 降级策略，为 P5-06 UI 展示和后续 importer 实现提供稳定协议。

## 1. 结论

P5-03 只定义历史 Run 只读导入方案，不实现真实 importer，不执行真实 Runner，不调用 Codex、OpenAI、TTS、HyperFrames 或 Pencil。

历史导入的核心原则：

- `RunSpec` 是导入后的运行根对象，但它代表 historical read-only run，不代表 Miracle 重新执行过该任务。
- `WorkflowSnapshot` 冻结 `content-production-real-v0` 在导入时的 WorkflowSpec 映射版本。
- `RunManifest` 是本地文件清单和 projection 索引，不是运行事实。
- `TraceEvent` 只能来自真实 `task_events.jsonl` 或后续 importer 自己写入的 `import_*` 审计事件，不能凭文件存在伪造执行事件。
- W24 作为主样本输出完整 historical projection；W23 作为降级样本，只输出基础 Artifact projection、缺口 Attention 和 source metadata。

## 2. Importer 定位

历史 importer 是 Miracle Local Sidecar 的本地转换器，职责是把真实项目交付包转换为 Miracle 可展示的只读对象。

| 项 | 说明 |
|---|---|
| 名称 | `historical-run-importer` |
| 模式 | `historical_readonly` |
| 首个 Domain | `content-production` |
| 首个 Workflow | `content-production-real-v0` |
| 主样本 | W24 真实交付包 |
| 对照样本 | W23 真实交付包 |
| 写入目标 | Miracle workspace 下的 `.miracle/runs/{run_id}` |
| 运行边界 | 只读扫描、hash/mtime 读取、对象 projection、审计事件写入 |
| 禁止行为 | 执行真实任务、调用 provider、复制大媒体、伪造缺失 TraceEvent、覆盖源工作区文件 |

## 3. 输入参数

建议 importer 接口在 P5 后续实现时采用显式参数，不从固定路径硬编码。

```json
{
  "mode": "historical_readonly",
  "workflow_id": "content-production-real-v0",
  "source_workspace": "/Users/zhangyue/Documents/Obsidian Vault/热点工具更新",
  "source_run_dir": "runs/real/2026-W24_Codex_ClaudeCode_真实任务交付包",
  "comparison_run_dir": "runs/real/2026-W23_Codex_ClaudeCode_真实任务交付包",
  "target_workspace": "fixtures/mvp-workspace/.miracle",
  "copy_policy": {
    "copy_text_artifacts": false,
    "copy_media_artifacts": false,
    "record_absolute_paths": true
  },
  "projection_policy": {
    "allow_inferred_artifacts": true,
    "allow_inferred_node_status": true,
    "allow_inferred_trace_events": false,
    "source_confidence_required": true
  }
}
```

参数规则：

| 字段 | 规则 |
|---|---|
| `workflow_id` | 必须指向 registry 中存在的 WorkflowSpec；P5 首个目标是 `content-production-real-v0`。 |
| `source_workspace` | 必须存在；用于把相对路径解析为绝对路径。 |
| `source_run_dir` | 必须在 `source_workspace` 下；不能读取任意外部目录。 |
| `target_workspace` | 指向 Miracle `.miracle` workspace；P5 MVP 默认使用 fixture workspace。 |
| `copy_text_artifacts` | P5 第一轮建议为 `false`，避免导入行为改变真实资料所有权。 |
| `copy_media_artifacts` | 必须为 `false`；MP4/WAV/JPG 只记录路径、size、mtime/hash 和 preview capability。 |
| `allow_inferred_trace_events` | 必须为 `false`；没有结构化事件证据时不能生成运行事件。 |

## 4. 输出目录

历史 Run 输出沿用现有 MVP run 目录结构，避免 UI 和 API 读取两套模型。

```text
fixtures/mvp-workspace/.miracle/runs/run-real-2026-W24-codex-claudecode/
  run_spec.json
  workflow_snapshot.json
  manifest.json
  nodes.json
  artifacts.json
  gates.json
  attention.json
  events.jsonl
  source_meta.json

fixtures/mvp-workspace/.miracle/runs/run-real-2026-W23-codex-claudecode/
  run_spec.json
  workflow_snapshot.json
  manifest.json
  nodes.json
  artifacts.json
  gates.json
  attention.json
  events.jsonl
  source_meta.json
```

新增 `source_meta.json` 只用于 historical importer，不替代核心对象。

## 5. 输出对象清单

| 输出 | W24 | W23 | 说明 |
|---|---|---|---|
| `run_spec.json` | 完整 | 降级 | historical read-only run 根对象。 |
| `workflow_snapshot.json` | 完整 | 完整 | 冻结 `content-production-real-v0` 映射版本。 |
| `manifest.json` | 完整 | 完整 | 指向 run_spec、snapshot、events、nodes、artifacts、gates、attention。 |
| `nodes.json` | C0/C/D/E/F observed，A/B/G inferred | 大部分 inferred | NodeRun projection，不等于真实执行回放。 |
| `artifacts.json` | 完整 | 基础 | ArtifactManifest projection，记录源路径和状态。 |
| `gates.json` | C/D/F/E observed，B/G inferred | 缺口为主 | GateInstance projection；GateDecision 只导入结构化证据。 |
| `attention.json` | F 待审、A/B/G inferred、可选 warnings | 缺控制文件、缺 trace、缺 gate decision | 根因聚合项。 |
| `events.jsonl` | 导入真实 task_events + importer 审计事件 | 只写 importer 审计事件 | 不伪造历史执行事件。 |
| `source_meta.json` | 完整 | 完整 | 每个 projection 对象的来源、confidence、source path。 |

## 6. RunSpec Projection

W24 建议生成：

```json
{
  "run_id": "run-real-2026-W24-codex-claudecode",
  "workflow_id": "content-production-real-v0",
  "workflow_version": "0.1.0",
  "workflow_snapshot_id": "snap-real-2026-W24-codex-claudecode",
  "status": "running",
  "execution_policy": "historical_readonly",
  "role_profile": "operator",
  "resolved_components": [
    "official-source-library",
    "fact-check-library",
    "content-packaging-library",
    "script-routing-library",
    "storyboard-library",
    "tts-caption-library",
    "hyperframes-library",
    "video-render-library",
    "distribution-library",
    "review-retro-library"
  ],
  "resolved_provider_policy": {
    "default_provider": "codex-local",
    "allowed_providers": ["codex-local", "official-api", "mock-provider", "manual-upload"],
    "required_credentials": ["VOLC_TTS_API_KEY", "VOLC_TTS_VOICE_TYPE"],
    "fallback_providers": ["mock-tts", "manual-upload"]
  },
  "created_at": "2026-06-12T00:00:00.000Z"
}
```

状态映射：

| 源状态 | RunSpec.status | 原因 |
|---|---|---|
| `F_final_render pending_review` | `running` | Run 仍有未完成审核门；具体待审状态由 GateInstance/ArtifactManifest 承载。 |
| 无控制状态但产物齐全 | `completed` 或 `running` 需人工确认 | 只可用于 W23 降级导入，不自动认定完成。 |
| 缺关键控制文件 | `paused` 或 `running` | 若无法确认审核门，优先进入 paused/attention，而不是 completed。 |

## 7. WorkflowSnapshot Projection

WorkflowSnapshot 由 `content-production-real-v0` 在导入时冻结，内容来自 P5-02 的对象映射。

冻结规则：

- 保存完整 nodes、edges、artifacts、gates、provider_policy 和 registry_meta。
- 不保存 UI 临时状态为执行依赖；Canvas/DAG layout 只影响展示。
- `snapshot_id` 必须进入 `run_spec.workflow_snapshot_id`。
- 后续 WorkflowSpec 变更不回写已导入 historical run。

## 8. RunManifest Projection

`manifest.json` 继续保持 P4 MVP 结构，并可扩展 historical metadata 路径。

```json
{
  "run_id": "run-real-2026-W24-codex-claudecode",
  "run_spec_path": "runs/run-real-2026-W24-codex-claudecode/run_spec.json",
  "workflow_snapshot_path": "runs/run-real-2026-W24-codex-claudecode/workflow_snapshot.json",
  "events_path": "runs/run-real-2026-W24-codex-claudecode/events.jsonl",
  "nodes_path": "runs/run-real-2026-W24-codex-claudecode/nodes.json",
  "artifacts_path": "runs/run-real-2026-W24-codex-claudecode/artifacts.json",
  "gates_path": "runs/run-real-2026-W24-codex-claudecode/gates.json",
  "attention_path": "runs/run-real-2026-W24-codex-claudecode/attention.json",
  "source_meta_path": "runs/run-real-2026-W24-codex-claudecode/source_meta.json"
}
```

## 9. 文件扫描与忽略规则

Importer 必须先扫描文件，再做对象映射。扫描结果写入 `source_meta.json`，不直接作为 TraceEvent。

忽略规则：

| 类型 | 规则 |
|---|---|
| 系统文件 | 忽略 `.DS_Store`、`Thumbs.db`。 |
| 临时文件 | 忽略 `*.tmp`、`*.bak`、`~$*`、编辑器 swap 文件。 |
| 构建缓存 | 忽略 `node_modules/`、`.cache/`、`dist/`、`build/`。 |
| 大媒体复制 | 不复制 `mp4/mov/mkv/webm/wav/mp3/m4a/aac/flac/aiff`，只记录 metadata。 |
| 预览图复制 | 不复制 `*_预览帧.jpg/png`，只记录 metadata 和 preview capability。 |
| 凭证文件 | 不读取 `.env`、key 文件或 token 文件；只记录 required credential name。 |

文件 metadata：

```json
{
  "source_path": "04_PPT视频/render_manifest.json",
  "absolute_path": "/Users/zhangyue/Documents/Obsidian Vault/热点工具更新/runs/real/2026-W24_Codex_ClaudeCode_真实任务交付包/04_PPT视频/render_manifest.json",
  "exists": true,
  "kind": "json",
  "size_bytes": 12345,
  "mtime": "2026-06-12T10:20:00.000Z",
  "hash": "sha256:...",
  "copied": false
}
```

## 10. NodeRun Projection

W24 NodeRun 映射：

| NodeSpec | 建议 status | 证据 | confidence |
|---|---|---|---|
| `A_fact_intelligence` | `done` | raw_items、clean_events 文件存在 | `inferred_from_file` |
| `B_md_master` | `done` | MD 母稿存在且被后续使用 | `inferred_from_file` |
| `C0_script_pool_selection` | `done` | `phase_status.md` auto-approved | `observed_from_status` |
| `C_ppt_storyboard` | `done` | `phase_status.md` approved + decision event | `observed_from_event` |
| `D_voiceover_audio` | `done` | `phase_status.md` approved + decision event | `observed_from_event` |
| `E_visual_video` | `done` | `phase_status.md` approved | `observed_from_status` |
| `F_final_render` | `reviewing` | `phase_status.md` pending_review + render_manifest | `observed_from_status` |
| `G_distribution_retro` | `queued` 或 `done` | 发布包/质检文件存在，但视频发布门未过 | `inferred_from_file` |

关键规则：

- inferred 的 `done` 必须在 source metadata 中标记，UI 应展示“由文件存在推断”。
- `F_final_render` 不得映射为 `done`，因为 `render_manifest.publish_allowed` 是 `pending_human_visual_review`。
- `G_distribution_retro` 若展示为 `done`，只能表示“MD 发布包草案存在”；不得表示“视频已允许发布”。
- W23 没有 phase status，所有 NodeRun 状态默认 `done/inferred` 或 `reviewing/inferred`，并生成缺控制文件 Attention。

## 11. ArtifactManifest Projection

ArtifactManifest 来源优先级：

1. `task_trace.json.artifacts`。
2. `render_manifest.json`。
3. 标准路径文件存在。
4. 目录扫描 fallback。

状态规则：

| 条件 | ArtifactManifest.status | review_status |
|---|---|---|
| 文件存在且无需审核 | `created` | `none` |
| 文件存在且 GateInstance pending | `created` | `pending_review` |
| 文件存在但只有推断证据 | `created` | `none`，可信度写入 `source_meta.confidence` |
| 文件应存在但缺失 | `missing` | `none` |
| 大媒体存在但不复制 | `created` | 按 Gate 映射 |

媒体策略：

- `path` 记录源文件绝对路径或 source workspace 相对路径。
- `copied` 必须为 `false`。
- `preview_capability` 可标记为 `local_path_preview`、`metadata_only` 或 `unsupported`。
- `hash` 可延后计算；若文件很大，P5 第一轮允许先记录 `size + mtime`，P5-06 前再决定是否计算 sha256。

## 12. Gate Projection

GateInstance 状态来自 P5-02 Gate 映射，P5-03 只定义导入边界。

W24 最小 Gate 输出：

| GateInstance | status | target | decisions | confidence |
|---|---|---|---|---|
| `gate-real-W24-C_ppt_storyboard` | `decided` | storyboard artifacts | 从 `approval_decisions.jsonl` 导入 approve decision | `observed_from_event` |
| `gate-real-W24-D_voiceover_audio` | `decided` | audio/timeline artifacts | 从 `approval_decisions.jsonl` 导入 approve decision | `observed_from_event` |
| `gate-real-W24-E_visual_video` | `decided` | HyperFrames handoff | 空数组；批准含义写入 `source_meta` | `observed_from_status` |
| `gate-real-W24-F_final_render` | `pending_review` | render/video artifacts | 空数组 | `observed_from_status` |
| `gate-real-W24-B_md_master` | `decided` | MD master | 空数组；等价通过含义写入 `source_meta` | `inferred_from_file` |
| `gate-real-W24-G_distribution` | `pending_review` 或 `decided` | publish package | 空数组；草案/完成含义写入 `source_meta` | `inferred_from_file` |

规则：

- GateDecision 只从 `approval_decisions.jsonl` 或明确的人审日志结构化记录导入。
- `pending_review` 属于 GateInstance，不属于 GateDecision。
- GateInstance.status 只能使用 `pending_review`、`decided`、`invalidated`；`approved/equivalent`、`draft`
  这类历史语义只能写入 `source_meta.confidence`、`source_meta.import_note` 或后续展示字段。
- W23 缺审核记录时，只生成 GateInstance 缺口 projection，不生成 GateDecision。

## 13. TraceEvent Projection

TraceEvent 分三类：

| 类型 | 是否写入 events.jsonl | 说明 |
|---|---|---|
| 源 `task_events.jsonl` | 是 | 按行转换并保留 source payload。 |
| importer 审计事件 | 是 | `import_started`、`import_completed`、`import_warning`、`import_gap_detected`。 |
| 文件存在推断事件 | 否 | 只能进 source metadata 或 Attention，不写入 TraceEvent。 |

W24 `events.jsonl` 可包含：

```json
{"event_id":"evt_import_started_W24","type":"import_started","run_id":"run-real-2026-W24-codex-claudecode","source":"historical-run-importer"}
{"event_id":"evt_source_001","type":"source_event_mirrored","run_id":"run-real-2026-W24-codex-claudecode","source_path":"00_任务控制/task_events.jsonl","source_line":1}
{"event_id":"evt_import_completed_W24","type":"import_completed","run_id":"run-real-2026-W24-codex-claudecode","source":"historical-run-importer"}
```

W23 `events.jsonl` 只能包含 importer 审计事件和缺口事件，例如：

```json
{"event_id":"evt_import_gap_W23_trace_missing","type":"import_gap_detected","run_id":"run-real-2026-W23-codex-claudecode","message":"task_events.jsonl missing; historical TraceEvent was not reconstructed"}
```

## 14. Attention Projection

Attention 采用根因聚合，而不是每个对象一张卡。

W24 建议 Attention：

| item | priority | root cause | 关联对象 |
|---|---|---|---|
| `att-real-W24-final-render-review` | P0 | 最终渲染等待人工视觉审看 | `F_final_render`、`F_final_render_gate`、video artifacts |
| `att-real-W24-inferred-early-phases` | P2 | A/B/G 缺标准 phase status，只能由文件存在推断 | A/B/G NodeRun、相关 artifacts |
| `att-real-W24-media-not-copied` | P3 | 大媒体只记录本地路径，不复制进 Miracle workspace | video/audio/image artifacts |

W23 建议 Attention：

| item | priority | root cause | 关联对象 |
|---|---|---|---|
| `att-real-W23-control-files-missing` | P1 | 缺 phase_status、task_trace、task_events、approval_decisions | run、nodes、gates |
| `att-real-W23-render-manifest-missing` | P2 | 缺 render_manifest，无法做视频校验 projection | video artifacts |
| `att-real-W23-gate-history-missing` | P2 | 缺结构化审核历史，不能还原 GateDecision | gates |

## 15. source_meta.json

`source_meta.json` 是 historical importer 的证据索引。建议结构：

```json
{
  "importer": "historical-run-importer",
  "importer_version": "0.1.0",
  "mode": "historical_readonly",
  "source_workspace": "/Users/zhangyue/Documents/Obsidian Vault/热点工具更新",
  "source_run_dir": "runs/real/2026-W24_Codex_ClaudeCode_真实任务交付包",
  "workflow_id": "content-production-real-v0",
  "run_id": "run-real-2026-W24-codex-claudecode",
  "imported_at": "2026-07-03T00:00:00.000Z",
  "objects": {
    "nr_real_W24_F_final_render": {
      "object_type": "NodeRun",
      "source_paths": ["00_任务控制/phase_status.md", "04_PPT视频/render_manifest.json"],
      "confidence": "observed_from_status",
      "import_note": "historical projection; not re-executed"
    }
  },
  "gaps": [
    {
      "code": "phase_status_missing_for_A_B_G",
      "severity": "warning",
      "message": "A/B/G lack standard phase status; states are inferred from artifact files"
    }
  ]
}
```

## 16. W24 主样本导入步骤

1. 校验 `source_workspace` 和 W24 `source_run_dir` 存在。
2. 扫描控制文件：`phase_status.md`、`approval_log.md`、`task_trace.json`、`task_events.jsonl`、`approval_decisions.jsonl`、`render_manifest.json`。
3. 扫描标准产物路径，忽略系统文件、缓存和大媒体复制。
4. 生成 `run_spec.json` 和 `workflow_snapshot.json`。
5. 根据 P5-02 映射生成 `nodes.json`、`artifacts.json`、`gates.json`。
6. 从 `task_events.jsonl` 镜像真实事件，从 importer 写入导入审计事件。
7. 生成 `attention.json` 和 `source_meta.json`。
8. 运行结构校验：所有 artifact 的 `node_run_id` 可解析，所有 Gate target 可解析，所有 required source metadata 存在。

## 17. W23 对照样本降级导入步骤

1. 校验 W23 目录存在。
2. 识别缺失控制文件并写入 `source_meta.gaps`。
3. 只基于文件存在生成 ArtifactManifest projection。
4. NodeRun 状态全部标记为 inferred，不生成 observed status。
5. 不生成 GateDecision；GateInstance 仅用于展示缺口。
6. `events.jsonl` 只写 importer 审计事件和 `import_gap_detected`。
7. 生成 Attention 汇总缺失控制文件，提示 W23 只能作为低可信度历史展示样本。

## 18. 验收标准

P5-03 方案通过条件：

| 验收项 | 结果 |
|---|---|
| 明确 historical importer 输入参数 | 通过 |
| 明确输出目录沿用现有 `.miracle/runs/{run_id}` 结构 | 通过 |
| 明确 `source_meta.json` 承载来源和可信度 | 通过 |
| 明确 W24 主样本 projection 规则 | 通过 |
| 明确 W23 降级导入规则 | 通过 |
| 明确不执行 Runner、不调用 provider、不复制媒体、不伪造事件 | 通过 |
| 明确 P5-04/P5-05/P5-06 的后续输入 | 通过 |

## 19. 对后续任务的输入

P5-04 应继续完成：

- `approval_policy.yaml` 到 GateSpec/GateInstance/GateDecision 的详细字段映射。
- B/G inferred gate 的命名和 UI 展示口径。
- `F_final_render pending_review` 的审核动作和恢复路径。

P5-05 应继续完成：

- `task_trace.json.steps` 到 NodeAttempt 的字段映射。
- `task_events.jsonl` 到 TraceEvent 的事件类型映射。
- 缺失事件时的 source metadata 与 Attention 规则。

P5-06 应在 P5-03/P5-04/P5-05 之后验证：

- Run 页面能展示 W24 historical run。
- Artifact Board 能展示本地真实产物路径和媒体 metadata。
- Gate 抽屉能展示 W24 final render pending review。
- Attention 能聚合 W24 待审和 W23 缺控制文件问题。

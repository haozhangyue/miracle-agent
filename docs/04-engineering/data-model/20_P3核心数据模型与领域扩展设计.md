# 20_P3核心数据模型与领域扩展设计

> 文档状态：P3 数据模型详细设计。
>
> 核心目标：让 Miracle 的核心对象能表达不同岗位、不同业务和不同工作流，而不是只表达 `content-production`。

## 1. 模型分层

Miracle 的数据模型分为四层：

```text
Domain Layer
-> Spec Layer
-> Run Layer
-> Projection Layer
```

| 层级 | 核心对象 | 职责 |
|---|---|---|
| Domain Layer | DomainPack、RoleProfile、ArtifactType、ComponentLibrary | 定义领域、岗位、产物和能力组合。 |
| Spec Layer | WorkflowTemplate、WorkflowSpec、NodeSpec、EdgeSpec、GateSpec、ProviderPolicy | 定义可版本化、可安装、可校验的工作流。 |
| Run Layer | RunSpec、WorkflowSnapshot、RunManifest、NodeRun、NodeAttempt、GateInstance、ArtifactManifest、TraceEvent | 记录一次运行的冻结事实和执行历史。 |
| Projection Layer | AttentionItem、AgentHealthProjection、ArtifactBoardItem、RunSummary | 把运行事实投影成 UI 可读视图。 |

原则：

1. Spec Layer 只描述计划，不记录执行结果。
2. Run Layer 只追加事实，不覆盖历史。
3. Projection Layer 可以重算，不作为唯一事实来源。
4. Domain Layer 可安装、可禁用、可版本升级。

运行对象边界：

| 对象 | 职责 | 是否核心真相 |
|---|---|---|
| `RunSpec` | 一次运行的根对象，冻结 workflow 版本、WorkflowSnapshot、resolved components、resolved ProviderPolicy 和启动参数。 | 是 |
| `WorkflowSnapshot` | 从 WorkflowSpec 复制出的不可变工作流副本，供本次 Run 恢复和审计使用。 | 是 |
| `RunManifest` | 本地文件清单或投影索引，记录 run 目录、workflow snapshot 路径、event journal 路径和 artifact 索引。 | 否 |

P3 不再引入 `RunSnapshot` 作为新的核心对象，避免与 `RunSpec / WorkflowSnapshot`
形成两套冻结模型。

## 2. DomainPack

`DomainPack` 定义一类业务领域的默认模板、角色、产物类型、组件库和推荐视图。

最小结构：

```yaml
id: content-production
name: 内容生产
version: 0.1.0
status: experimental
categories:
  - media
  - publishing
artifact_types:
  - markdown
  - script
  - audio
  - video
  - publish_package
role_profiles:
  - operator
  - editor
  - reviewer
workflow_templates:
  - content-production-v0
component_libraries:
  - official-source-library
  - content-packaging-library
  - tts-caption-library
default_views:
  home: operator
  run: full
  attention: risk
```

DomainPack 只提供默认配置，不改变核心对象语义。

示例：

| DomainPack | 默认模板 | 典型角色 | 典型产物 |
|---|---|---|---|
| `content-production` | content-production-v0 | operator、editor、reviewer | markdown、script、audio、video |
| `image-generation` | image-generation-v0 | designer、reviewer | prompt、image、upscale_result |
| `research-analysis` | research-report-v0 | researcher、reviewer | dataset、evidence_table、report |
| `script-writing` | drama-script-v0 | writer、director、reviewer | character_sheet、outline、episode_script |
| `sales-ops` | sales-followup-v0 | sales、manager、compliance | lead_list、message_pack、crm_note |

## 3. RoleProfile

`RoleProfile` 定义岗位视角，不等同于账号权限。

最小结构：

```yaml
id: editor
name: 编辑
default_landing: artifact_board
visible_modules:
  - home
  - run
  - attention
  - gate_review
primary_objects:
  - ArtifactManifest
  - GateInstance
  - AttentionItem
default_filters:
  artifact_kind:
    - markdown
    - script
  gate_status:
    - pending_review
actions:
  - review_artifact
  - request_rework
  - approve_gate
```

RoleProfile 用于：

1. 决定默认首页摘要。
2. 决定页面默认过滤器。
3. 决定动作按钮是否展示。
4. 决定后续商业化权限模型的输入。

MVP 不做账号系统，但 RoleProfile 需要先在数据模型中存在。

## 4. WorkflowTemplate 与 WorkflowSpec

`WorkflowTemplate` 是可安装模板；`WorkflowSpec` 是项目中的可编辑工作流。

关系：

```text
WorkflowTemplate
-> install / clone
-> WorkflowSpec
-> validate / dry-run
-> RunSpec
-> WorkflowSnapshot
```

最小结构：

```yaml
template_id: content-production-v0
domain: content-production
name: 内容生产全流程
version: 0.6.0
status: stable
entry_roles:
  - operator
  - editor
tags:
  - publishing
  - video
required_libraries:
  - official-source-library
  - content-packaging-library
nodes:
  - id: collect_sources
    type: source
    capability_requirements:
      - source.collect
  - id: generate_primary_artifact
    type: transform
    capability_requirements:
      - content.longform_draft
edges:
  - from: collect_sources
    to: generate_primary_artifact
    required: true
    join_policy:
      wait_if_active: false
      on_no_qualified_artifact: block_downstream
```

禁止：

1. 在核心字段中写 `Flow A`、`Flow B` 等固定阶段。
2. 在核心字段中写 `md_master`、`tts_video_branch` 等领域专属语义。
3. 让下游页面依赖节点 id 判断业务含义。

允许：

1. 在 `metadata.domain_labels` 中保留领域展示名。
2. 在 `layouts` 中保留视觉分组。
3. 在 `ArtifactType` 中定义领域产物类型。

## 5. NodeSpec

`NodeSpec` 描述一个工作节点。

最小字段：

```yaml
id: generate_visual_asset
name: 生成视觉资产
type: transform
domain_tags:
  - image-generation
capability_requirements:
  - prompt.optimize
  - image.generate
recommended_libraries:
  - image-generation-library
agent_candidates:
  - visual-agent
inputs:
  - id: creative_brief
    kind: artifact
    artifact_type: document
    required: true
outputs:
  - id: generated_images
    kind: artifact
    artifact_type: image
    required: true
    artifact_spec_ref: generated_images_artifact
review_gate_ref: generated_images_review_gate
failure_policy:
  retry: 1
  on_missing_input: blocked
  on_provider_failure: failed
```

节点类型保持少量通用枚举：

| type | 用途 |
|---|---|
| `start` | 任务入口。 |
| `source` | 数据源、用户输入、外部输入。 |
| `transform` | 生成、清洗、改写、转换。 |
| `agent` | Agent 执行任务。 |
| `tool` | 调用普通工具或脚本。 |
| `mcp_tool` | 调用 MCP tool。 |
| `branch` | 条件分支。 |
| `loop` | 循环或批量处理。 |
| `review_gate` | 人工或自动审核。 |
| `artifact` | 显式产物节点。 |
| `subworkflow` | 子工作流入口。 |
| `end` | 正常结束。 |
| `terminate` | 显式失败或中止。 |

NodeSpec 规则：

1. NodeSpec 只声明节点输入、输出、能力需求、候选 Agent、失败策略和可选 `review_gate_ref`。
2. `review_gate_ref` 只是便捷引用，用于 UI 和 dry-run 提示，不是审核真相。
3. 真正审核策略必须放在 `ArtifactSpec.review_policy` 或 `GateSpec` 中。
4. 一个节点产出多个 Artifact 时，每个 Artifact 可绑定不同 Gate，不能把审核策略粗暴挂在节点自身。

## 6. EdgeSpec 与 join_policy

`EdgeSpec` 描述节点间依赖。执行依赖只看 EdgeSpec，不看 layout 或阶段名。

最小字段：

```yaml
from: generate_primary_artifact
to: publish_package
artifact_selector:
  artifact_type: markdown
  review_status: approved
required: true
join_policy:
  wait_if_active: true
  max_wait: PT10M
  on_timeout: continue_if_required_inputs_ready
  on_no_qualified_artifact: block_downstream
```

字段说明：

| 字段 | 用途 |
|---|---|
| `required` | 是否为下游启动所需的必需边。 |
| `artifact_selector` | 声明下游需要哪类产物或审核状态。 |
| `join_policy.wait_if_active` | 可选分支仍在运行时是否等待。 |
| `join_policy.max_wait` | 最长等待时间。 |
| `join_policy.on_timeout` | 超时后继续、阻塞、失败或进入人工决策。 |
| `join_policy.on_no_qualified_artifact` | 没有合格产物时的处理方式。 |

推荐枚举：

```text
on_timeout:
  continue_if_required_inputs_ready / blocked / failed / require_decision
on_no_qualified_artifact:
  ignore_optional / block_downstream / require_decision
```

内容生产样例中，Markdown 分发使用 `B -> G required`，视频分发使用 `F -> G optional`。
因此 TTS 或视频分支异常只能影响视频产物，不应阻塞 Markdown required 主链路。

## 7. ArtifactType、ArtifactSpec 与 ArtifactManifest

`ArtifactType` 定义产物类型；`ArtifactSpec` 定义工作流期望产物和审核策略；
`ArtifactManifest` 记录一次运行中真实产物。

ArtifactType 示例：

```yaml
id: image
name: 图片
mime_groups:
  - image/png
  - image/jpeg
preview:
  default_view: gallery
allowed_actions:
  - review
  - version
  - export
  - upscale
```

通用 ArtifactType：

```text
markdown / json / image / video / audio / document / dataset /
prototype / script / storyboard / prompt / publish_package / report
```

ArtifactSpec 示例：

```yaml
id: generated_images_artifact
type: image
produced_by: generate_visual_asset
review_policy:
  mode: manual
  gate_spec_id: generated_images_review_gate
required_for:
  - visual_delivery
versioning:
  immutable: true
  compare_by: hash
```

ArtifactManifest 最小字段：

```yaml
artifact_id: art_001
run_id: run_20260626_001
node_run_id: nr_generate_visual_asset
type: image
version: 1
path: artifacts/generated_images/image_001.png
hash: sha256:...
status: created
review_status: pending_review
created_at: 2026-06-26T10:00:00+08:00
metadata:
  provider: images-create
  prompt_artifact: art_prompt_001
```

规则：

1. 产物版本递增，不覆盖旧版本。
2. Gate 审核只改变审核事实，不直接重写产物。
3. 删除动作在 MVP 中只做隐藏或标记，不物理删除。
4. `ArtifactSpec.review_policy` 是审核绑定真相；`GateInstance/GateDecision` 是运行期审核事实。

## 8. AgentSpec 与 AgentHealthProjection

`AgentSpec` 是能力定义；`AgentHealthProjection` 是运行状态投影。

AgentSpec：

```yaml
id: visual-agent
name: 视觉 Agent
role: visual_production
equipped_libraries:
  - image-generation-library
  - prompt-optimization-library
permissions:
  can_read:
    - artifact:prompt
    - artifact:image
  can_produce:
    - artifact:image
  can_emit:
    - adapter_result
runtime_adapters:
  - official-api
  - local-sidecar
```

AgentHealthProjection：

```yaml
agent_id: visual-agent
status: running
active_runs:
  - run_20260626_001
current_node_runs:
  - nr_generate_visual_asset
queued_node_runs: []
blocked_reason: null
waiting_for: []
heartbeat_at: 2026-06-26T10:02:00+08:00
equipped_libraries:
  - image-generation-library
```

P3 需要避免把 Agent 建模成“一次只能执行一个任务”。MVP UI 可以只展示主任务，但数据模型必须能容纳并发任务列表。

权限规则：

1. Agent 和 Adapter 不能直接写 `TraceEvent`、`Event Journal`、`NodeRun` 或 `ArtifactManifest`。
2. Agent 和 Adapter 只能返回 `AdapterResult`、artifact descriptor 和候选事件摘要。
3. Sidecar 内的 Orchestrator 是运行事实唯一写入方，负责把 AdapterResult 转成 TraceEvent 和投影更新。

## 9. AttentionItem

`AttentionItem` 是由运行事实投影出的处置项。

最小字段：

```yaml
attention_id: att_tts_credential
root_cause_key: credential:VOLC_TTS_API_KEY:missing
title: TTS 凭证缺失
severity: P0
status: open
source_events:
  - evt_001
related_objects:
  - type: NodeRun
    id: nr_tts
  - type: AgentHealth
    id: tts-agent
  - type: ArtifactManifest
    id: subtitle_master_v1
impact:
  blocked_nodes:
    - nr_tts
  waiting_agents:
    - video-agent
  unaffected_paths:
    - markdown_distribution
safe_actions:
  - configure_credential
  - switch_provider
  - skip_optional_branch
```

生命周期：

```text
open -> acknowledged -> snoozed -> resolved
```

规则：

1. 一个根因对应一个主 AttentionItem。
2. 关联对象作为展开信息，不重复生成多张主卡。
3. 安全动作必须声明前置条件和影响范围。
4. 高风险动作不能通过“静音”替代修复。

## 10. GateInstance 与 GateDecision

`GateInstance` 是审核实例；`GateDecision` 是审核决策。

最小结构：

```yaml
gate_instance_id: gate_artifact_review_001
run_id: run_20260626_001
gate_spec_id: review_primary_artifact
target:
  type: ArtifactManifest
  id: art_report_v1
status: pending_review
required_before:
  - publish_package
decisions:
  - decision_id: gd_001
    actor: user
    decision: reject
    comment: 引用证据不足
    created_at: 2026-06-26T10:08:00+08:00
```

规则：

1. `GateDecision` 不覆盖 `GateInstance` 历史。
2. 驳回后创建新的 `NodeAttempt` 或新 Artifact version。
3. `pending_review` 属于 GateInstance 或 ArtifactManifest，不属于 GateDecision。

## 11. 示例校验

### 11.1 生图任务流

```text
需求输入 -> Prompt 策划 -> 图像生成 -> 审核 -> 放大/变体 -> 交付
```

可表达对象：

| 步骤 | NodeSpec | ArtifactType |
|---|---|---|
| 需求输入 | source | document |
| Prompt 策划 | transform | prompt |
| 图像生成 | agent/tool | image |
| 审核 | review_gate | review_note |
| 放大/变体 | transform | image |
| 交付 | artifact/end | publish_package |

### 11.2 研究分析任务流

```text
问题定义 -> 资料采集 -> 事实核验 -> 分析报告 -> 引用审计
```

可表达对象：

| 步骤 | NodeSpec | ArtifactType |
|---|---|---|
| 问题定义 | start/source | document |
| 资料采集 | source/agent | dataset |
| 事实核验 | transform/review_gate | evidence_table |
| 分析报告 | transform | report |
| 引用审计 | review_gate | citation |

### 11.3 内容生产 Flow A-G

Flow A-G 作为 `content-production-v0` 的一个模板实例表达，不进入核心模型硬编码。

可保留：

1. 展示标签 `Flow A-G`。
2. 模板名 `content-production-v0`。
3. 默认节点分组。

不可保留为核心模型：

1. 固定字段名 `flow_a` 到 `flow_g`。
2. 固定页面判断逻辑。
3. 只适用于内容生产的运行状态枚举。

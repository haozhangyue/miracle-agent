# 41_P5-04审核策略映射设计

> 任务状态：P5-04 已完成。
>
> 输入依据：`approval_policy.yaml`、W24 `phase_status.md`、`approval_log.md`、
> `approval_decisions.jsonl`、`task_events.jsonl`、`task_trace.json`、
> `render_manifest.json`、`38_P5-02FlowAG对象映射设计.md`、`39_P5-03历史Run只读导入方案.md`。
>
> 目标：将真实“热点工具更新”的审核策略映射为 Miracle 的 `GateSpec`、
> `GateInstance`、`GateDecision`、`ArtifactManifest.review_status` 和
> `source_meta` 规则，并明确哪些状态是真实审核事实，哪些只是 historical projection。

## 1. 结论

P5-04 的核心结论：

- `approval_policy.yaml` 是 Flow A-G 审核策略的 policy source，不是某一次 Run 的事实来源。
- `phase_status.md` 是 W24 run 的阶段状态来源，可生成 `GateInstance.status` 和
  `ArtifactManifest.review_status` projection。
- `approval_decisions.jsonl` 是 W24 结构化人工审核决策来源；只有这里或等价结构化日志中的记录可以生成 `GateDecision`。
- `approval_log.md` 是人读审核记录，可作为 `GateDecision.comment/evidence_files` 的辅助证据，但不单独生成结构化决策。
- `render_manifest.json.publish_allowed=pending_human_visual_review` 与
  `phase_status.md` 中 `F_final_render=pending_review` 一致，应映射为
  `F_final_render_gate.status=pending_review`，不能映射为 approved 或 completed。
- B/G 这类没有结构化 phase status 的阶段，只能生成 inferred gate projection，不生成
  `GateDecision`。
- `NodeSpec` 不新增 `review_policy`。审核真相仍在 `ArtifactSpec.review_policy`、
  `GateSpec`、`GateInstance`、`GateDecision` 和 `source_meta`。

## 2. 审核源文件分工

| 文件 | Miracle 作用 | 是否生成运行事实 | 说明 |
|---|---|---:|---|
| `config/workflow/approval_policy.yaml` | 生成 WorkflowSpec 中的 `ArtifactSpec.review_policy` 和 `GateSpec` | 否 | policy source，不代表某次 Run 已审核。 |
| W24 `phase_status.md` | 生成 `GateInstance.status`、NodeRun projection、Artifact review projection | 是，限状态 projection | `approved/auto-approved/pending_review` 可作为状态证据。 |
| W24 `approval_decisions.jsonl` | 生成 `GateDecision` | 是 | 每行是结构化审核事实。 |
| W24 `approval_log.md` | 补充评论、证据文件和人读说明 | 辅助 | 不单独生成 `GateDecision`，避免 Markdown 解析歧义。 |
| W24 `task_events.jsonl` | P5-05 映射 TraceEvent | 是，限 Trace | P5-04 只引用其中审核相关证据，不直接设计 Trace 映射。 |
| W24 `render_manifest.json` | 补充 F final render gate 状态、媒体输出、发布许可 | 是，限 projection | `publish_allowed` 是视觉发布门状态。 |
| W23 控制文件缺失 | 生成缺口 Attention 和低可信 source_meta | 否 | W23 不能生成 `GateDecision`。 |

## 3. approval_policy 到 Miracle 对象

### 3.1 顶层字段映射

| approval_policy 字段 | Miracle 映射 | 规则 |
|---|---|---|
| `version` | `WorkflowSpec.registry_meta.policy_version` 或 `source_meta.policy_version` | P5 MVP 可先进入 `source_meta`，不改变核心 schema。 |
| `default_mode` | `ArtifactSpec.review_policy.mode` fallback | phase 缺 `review_mode` 时使用。 |
| `allowed_review_modes` | validate 规则 | 只能是 `manual/auto/conditional/skip`。 |
| `allowed_statuses` | importer 状态映射白名单 | 非白名单状态进入 import warning，不进入核心对象。 |
| `downstream_read_rule.allow_statuses` | `EdgeSpec.artifact_selector.review_status=approved` + `source_meta.original_status` | Miracle 当前 `ArtifactReviewStatus` 没有 `auto-approved`，因此 `auto-approved` 映射为 `approved`，原始状态保留在 `source_meta`。 |
| `downstream_read_rule.reject_pending_drafts` | `GateInstance.pending_review` 阻断下游 | pending draft 不进入 downstream qualified artifact。 |
| `downstream_read_rule.rejected_requires_revision_note` | Gate reject/rework 要求 | P4 rework 模型已支持新 Artifact version。 |
| `downstream_read_rule.skipped_requires_reason` | skip projection metadata | P5 若遇 skipped，必须写 source_meta reason。 |
| `templates.*` | importer 输入路径 | 不进入 WorkflowSpec 执行模型。 |

### 3.2 review_mode 映射

| review_mode | ArtifactSpec.review_policy | GateSpec | GateInstance 生成规则 | GateDecision 生成规则 |
|---|---|---|---|---|
| `manual` | `manual + gate_spec_id` | 必须生成 | 每个 historical run 需生成 GateInstance | 只从结构化人工决策生成。 |
| `auto` | `auto` | 可不生成，或生成 auto gate projection | auto 检查通过时 Artifact review_status 映射为 `approved` | 不生成 `GateDecision`，除非后续有结构化 auto-decision 类型。 |
| `conditional` | `conditional + gate_spec_id` | 必须生成 | 根据 condition 和 run 状态生成 pending/decided/inferred | 只有结构化人工决策才生成。 |
| `skip` | `none` 或跳过 ArtifactSpec gate | 不生成 | NodeRun 可映射为 `skipped` | 不生成。 |

关键限制：

- Miracle 当前 `GateDecision.decision` 只有 `approve/reject/request_changes`，没有
  `auto_approve` 或 `skip`。
- `auto-approved` 是源系统 phase status，不是 Miracle `GateDecision`。
- `conditional` 不等于自动通过；它需要把 condition 结果写入 `source_meta.condition_result`。

## 4. allowed_statuses 到 Miracle 状态

| 源状态 | NodeRun.status | ArtifactManifest.review_status | GateInstance.status | GateDecision | source confidence |
|---|---|---|---|---|---|
| `not_started` | `waiting` 或 `queued` | `none` | 不生成或 pending gap | 不生成 | `observed_from_status` |
| `in_progress` | `running` | `none` | 不生成 | 不生成 | `observed_from_status` |
| `pending_review` | `reviewing` | `pending_review` | `pending_review` | 不生成 | `observed_from_status` |
| `approved` | `done` | `approved` | `decided` | 有结构化决策才生成 | `observed_from_event` 或 `observed_from_status` |
| `auto-approved` | `done` | `approved` | 可不生成或 `decided` projection | 不生成 | `observed_from_status` |
| `rejected` | `blocked` 或 `reviewing` | `rejected` | `decided` | 有结构化决策才生成 | `observed_from_event` 或 `observed_from_status` |
| `blocked` | `blocked` | `none` | 不生成或 pending gap | 不生成 | `observed_from_status` |
| `skipped` | `skipped` | `none` | 不生成 | 不生成 | `observed_from_status` |

`approved` 与 `auto-approved` 都能满足真实系统的 downstream read rule，但在 Miracle
核心对象里统一表现为 `ArtifactManifest.review_status=approved`。区别保留在
`source_meta.original_phase_status`，避免扩展核心枚举。

## 5. GateSpec 详细映射

| GateSpec id | review_mode | target_artifact_ref | required_before | actions | reject_to_phase |
|---|---|---|---|---|---|
| `B_md_master_gate` | manual | `md_master_artifact` | `C0_script_pool_selection`、`G_distribution_retro` | approve、reject、request_changes | `B_md_master` |
| `C_ppt_storyboard_gate` | manual | `storyboard_manifest_artifact` | `D_voiceover_audio` | approve、reject、request_changes | `C_ppt_storyboard` |
| `D_voiceover_audio_gate` | manual | `audio_manifest_artifact` | `E_visual_video` | approve、reject、request_changes | `D_voiceover_audio` |
| `E_visual_video_gate` | conditional | `hyperframes_handoff_artifact` | `F_final_render` | approve、reject、request_changes | `E_visual_video` |
| `F_final_render_gate` | manual | `render_manifest_artifact` | `G_distribution_retro` | approve、reject、request_changes | `F_final_render` |
| `G_distribution_gate` | conditional | `publish_package_artifact` | `external_publish` | approve、reject、request_changes | `G_distribution_retro` |

说明：

- A/C0 的 auto review 默认不生成人工 GateSpec；C0 的 auto 检查结果进入
  `source_meta.auto_checks` 和 Artifact review projection。
- E/G 是 conditional gate。若 condition 触发 manual，则按 manual gate 处理；若 condition
  触发 auto，则只生成状态 projection，不生成人工 `GateDecision`。
- `required_before=external_publish` 是 UI/导入层语义，不是 NodeSpec id；P5 importer
  应在 source_meta 中保留，P5-06 UI 展示时作为发布门提示。

## 6. W24 GateInstance 映射

| GateInstance id | GateSpec | target | status | decisions | confidence | 证据 |
|---|---|---|---|---|---|---|
| `gate-real-W24-B_md_master` | `B_md_master_gate` | `art-real-W24-md-master` | `decided` | `[]` | `inferred_from_file` | MD 母稿存在且被 C0/G 使用。 |
| `gate-real-W24-C_ppt_storyboard` | `C_ppt_storyboard_gate` | `art-real-W24-storyboard-manifest` | `decided` | 1 个 approve | `observed_from_event` | `approval_decisions.jsonl` 第 1 行、`approval_log.md`、`phase_status.md`。 |
| `gate-real-W24-D_voiceover_audio` | `D_voiceover_audio_gate` | `art-real-W24-audio-manifest` | `decided` | 1 个 approve | `observed_from_event` | `approval_decisions.jsonl` 第 2 行、`approval_log.md`、`phase_status.md`。 |
| `gate-real-W24-E_visual_video` | `E_visual_video_gate` | `art-real-W24-hyperframes-handoff` | `decided` | `[]` | `observed_from_status` | `phase_status.md` 中 E 为 approved。 |
| `gate-real-W24-F_final_render` | `F_final_render_gate` | `art-real-W24-render-manifest` | `pending_review` | `[]` | `observed_from_status` | `phase_status.md` 中 F 为 pending_review；`render_manifest.publish_allowed=pending_human_visual_review`。 |
| `gate-real-W24-G_distribution` | `G_distribution_gate` | `art-real-W24-publish-package` | `pending_review` 或 `decided` projection | `[]` | `inferred_from_file` | 发布包和质检文件存在，但缺结构化 G phase status。 |

G 阶段建议：

- P5-04 不自动认定 G 已发布通过。
- 如果 UI 需要展示，可命名为 `gate-real-W24-G_distribution_inferred`，并显示
  “发布包草案存在，缺结构化发布门决策”。
- G 不阻断 MD 发布包草案展示，但阻断“正式 external publish allowed”语义。

## 7. W24 GateDecision 映射

W24 只有两条结构化审核决策，应生成两个 `GateDecision`。

### 7.1 C_ppt_storyboard approve

```json
{
  "decision_id": "gd-real-W24-C_ppt_storyboard-20260611T170449",
  "actor": "user",
  "decision": "approve",
  "comment": "分镜草案通过，允许进入 TTS timeline 适配。",
  "created_at": "2026-06-11T17:04:49+0800"
}
```

source metadata：

```json
{
  "source_paths": [
    "00_任务控制/approval_decisions.jsonl:1",
    "00_任务控制/approval_log.md",
    "00_任务控制/phase_status.md"
  ],
  "source_decision_status": "valid",
  "review_mode": "manual",
  "reject_to_phase": "C_ppt_storyboard",
  "evidence_files": [
    "04_PPT视频/分镜草案/storyboard_adapter_manifest.json",
    "04_PPT视频/分镜草案/横版分镜草案.md",
    "04_PPT视频/分镜草案/竖版分镜草案.md",
    "04_PPT视频/分镜草案/图文轮播分镜草案.md",
    "04_PPT视频/PPT页纲_草案.md"
  ],
  "confidence": "observed_from_event"
}
```

### 7.2 D_voiceover_audio approve

```json
{
  "decision_id": "gd-real-W24-D_voiceover_audio-20260612T095653",
  "actor": "user",
  "decision": "approve",
  "comment": "新版 19 段云舟 2.0 配音听审通过，允许进入 HyperFrames 音画桥接。",
  "created_at": "2026-06-12T09:56:53+0800"
}
```

source metadata：

```json
{
  "source_paths": [
    "00_任务控制/approval_decisions.jsonl:2",
    "00_任务控制/approval_log.md",
    "00_任务控制/phase_status.md"
  ],
  "source_decision_status": "valid",
  "review_mode": "manual",
  "reject_to_phase": "D_voiceover_audio",
  "evidence_files": [],
  "evidence_completeness": "decision_valid_but_evidence_files_empty",
  "confidence": "observed_from_event"
}
```

注意：D 的 `evidence_files=[]` 不会让决策失效。它只表示历史审核记录没有列出具体证据文件，
UI 应提示“决策有效，证据文件列表为空”。

## 8. F_final_render pending_review 映射

F 是 P5-04 最关键的真实审核门。

源证据：

| 文件 | 字段 | 值 |
|---|---|---|
| `phase_status.md` | `F_final_render.status` | `pending_review` |
| `phase_status.md` | `summary` | `04_PPT视频/render_manifest.json` |
| `render_manifest.json` | `status` | `completed_pending_human_visual_review` |
| `render_manifest.json` | `publish_allowed` | `pending_human_visual_review` |
| `render_manifest.json` | `validation.ffprobe/resolution/fps/audio_track/preview_frames` | `passed` |
| `render_manifest.json` | `next_review_gate.status` | `pending_human_visual_review` |
| `task_events.jsonl` | `phase_pending_review` | `reason=waiting_human_visual_review` |

Miracle projection：

```json
{
  "gate_instance_id": "gate-real-W24-F_final_render",
  "run_id": "run-real-2026-W24-codex-claudecode",
  "gate_spec_id": "F_final_render_gate",
  "target": {
    "type": "ArtifactManifest",
    "id": "art-real-W24-render-manifest"
  },
  "status": "pending_review",
  "required_before": ["G_distribution_retro"],
  "decisions": []
}
```

Artifact projection：

```json
{
  "artifact_id": "art-real-W24-render-manifest",
  "review_status": "pending_review",
  "status": "created",
  "path": "04_PPT视频/render_manifest.json"
}
```

Attention projection：

```json
{
  "attention_id": "att-real-W24-final-render-review",
  "root_cause_key": "gate:F_final_render:pending_human_visual_review",
  "title": "最终渲染等待人工视觉审看",
  "severity": "P0",
  "status": "open",
  "related_objects": [
    { "type": "GateInstance", "id": "gate-real-W24-F_final_render" },
    { "type": "ArtifactManifest", "id": "art-real-W24-render-manifest" }
  ],
  "safe_actions": [
    "open_gate_review",
    "inspect_horizontal_video",
    "inspect_vertical_video",
    "approve_or_request_changes"
  ]
}
```

恢复路径：

| 操作 | 写入结果 | 下游影响 |
|---|---|---|
| approve | 新增 `GateDecision.approve`，GateInstance -> `decided`，render artifacts -> `approved` | `F -> G` optional video enhancement 可作为 approved input。 |
| request_changes | 新增 `GateDecision.request_changes`，target artifact -> `rejected`，GateInstance -> `decided` | 创建返工版本，G 不读取该视频产物。 |
| reject | 新增 `GateDecision.reject`，target artifact -> `rejected`，GateInstance -> `decided` | 创建返工版本或跳过视频增强分支。 |

P5 historical importer 只导入 pending 状态，不主动执行上述操作。

## 9. B/G inferred gate 展示口径

### 9.1 B_md_master

B 的真实情况：

- `approval_policy.yaml` 中 B 是 manual。
- W24 没有标准 B phase status 和结构化 B GateDecision。
- `MD母稿_公众号知乎版.md` 存在，并被 C0 和 G 产物使用。

导入规则：

| 对象 | 建议 |
|---|---|
| GateInstance | 生成 `decided` projection，或者在 P5-06 UI 中弱化为 inferred gate。 |
| GateDecision | 不生成。 |
| ArtifactManifest.review_status | `approved`，但 source_meta 标记 `inferred_from_file`。 |
| Attention | 建议进入 P2：早期阶段缺标准 phase status。 |

UI 文案：

```text
MD 母稿被后续链路使用，按文件证据推断为等价通过；缺结构化 GateDecision。
```

### 9.2 G_distribution_retro

G 的真实情况：

- `approval_policy.yaml` 中 G 是 conditional。
- W24 有发布包和质检复盘文件。
- 缺结构化发布门状态和 GateDecision。
- F 视频仍是 pending_review，因此 G 不应显示“视频发布已批准”。

导入规则：

| 对象 | 建议 |
|---|---|
| GateInstance | 可生成 `pending_review` projection 或 inferred decided draft projection，P5 MVP 推荐 `pending_review`。 |
| GateDecision | 不生成。 |
| ArtifactManifest.review_status | `none` 或 `pending_review`，取决于 publish package 是否被视为 external publish target。 |
| Attention | 若 G 进入 UI，提示“发布门缺结构化决策”。 |

UI 文案：

```text
发布包和质检文件存在，但缺结构化发布门决策；视频增强分支仍等待最终渲染审看。
```

## 10. W23 降级样本规则

W23 缺少标准控制文件：

- 无 `phase_status.md`。
- 无 `approval_log.md`。
- 无 `approval_decisions.jsonl`。
- 无 `task_trace.json`。
- 无 `task_events.jsonl`。

因此：

| 对象 | W23 规则 |
|---|---|
| GateSpec | 仍来自 `approval_policy.yaml` 和 `content-production-real-v0`。 |
| GateInstance | 只生成 gap projection，不能作为真实审核状态。 |
| GateDecision | 不生成。 |
| ArtifactManifest.review_status | 不从文件存在推断为 approved；优先 `none`，必要时 source_meta 写 `inferred_from_file`。 |
| Attention | 必须生成 `att-real-W23-gate-history-missing`。 |

W23 只用于验证 importer 能处理低证据历史包，不能作为审核链路真实性样本。

## 11. source_meta 扩展

P5-04 建议在 `source_meta.json` 中为 Gate 相关对象加入以下字段。

```json
{
  "gate-real-W24-F_final_render": {
    "object_type": "GateInstance",
    "source_paths": [
      "00_任务控制/phase_status.md",
      "04_PPT视频/render_manifest.json",
      "00_任务控制/task_events.jsonl"
    ],
    "confidence": "observed_from_status",
    "policy_source": "config/workflow/approval_policy.yaml",
    "policy_phase": "F_final_render",
    "review_mode": "manual",
    "original_phase_status": "pending_review",
    "publish_allowed": "pending_human_visual_review",
    "import_note": "Final render files exist and validation passed, but human visual review is still pending."
  }
}
```

GateDecision metadata：

```json
{
  "gd-real-W24-C_ppt_storyboard-20260611T170449": {
    "object_type": "GateDecision",
    "source_paths": ["00_任务控制/approval_decisions.jsonl:1"],
    "confidence": "observed_from_event",
    "source_decision_status": "valid",
    "evidence_completeness": "evidence_files_listed"
  }
}
```

## 12. Importer 校验规则

P5 后续 importer 实现时，审核映射必须校验：

1. `approval_policy.version` 存在，`allowed_review_modes` 和 `allowed_statuses` 非空。
2. 每个 `manual` phase 若生成 `GateSpec`，必须有 `target_artifact_ref` 和 `required_before`。
3. `GateInstance.target.id` 必须能在 `artifacts.json` 中找到。
4. `GateDecision.decision` 只能是 `approve/reject/request_changes`。
5. `pending_review` 不得写入 `GateDecision.decision`。
6. `approved/auto-approved` 源状态映射为 `ArtifactManifest.review_status=approved` 时，必须在 source_meta 记录 `original_phase_status`。
7. 缺结构化决策时不能生成空壳 `GateDecision`。
8. F final render 只要 `publish_allowed=pending_human_visual_review`，就必须保持
   `GateInstance.status=pending_review`。
9. W23 这类缺控制文件样本必须生成缺口 Attention。

## 13. P5-05 输入

P5-05 Trace 映射可以基于本文件继续处理：

- `task_events.jsonl` 中 `phase_approved`、`phase_pending_review`、`final_render_completed`
  的 TraceEvent 类型映射。
- GateDecision 与 TraceEvent 的关联方式：`GateDecision` 由 `approval_decisions.jsonl`
  生成，TraceEvent 由 `task_events.jsonl` 生成，二者通过 phase/gate id 关联，不互相替代。
- `approval_log.md` 的 Markdown 内容仅作为 source payload 或 evidence note，不作为单独事件事实。
- F final render 的 `phase_pending_review` 应产生 Attention 关联事件，但不生成
  GateDecision。

## 14. 验收结果

| 验收项 | 结果 |
|---|---|
| `approval_policy.yaml` 字段已映射到 Gate 模型 | 通过 |
| `manual/auto/conditional/skip` 映射边界明确 | 通过 |
| W24 GateInstance 和 GateDecision 规则明确 | 通过 |
| F_final_render pending_review 映射和恢复路径明确 | 通过 |
| B/G inferred gate 展示口径明确 | 通过 |
| W23 缺审核历史降级规则明确 | 通过 |
| 不把审核策略写回 NodeSpec | 通过 |
| 不伪造缺失 GateDecision 或 TraceEvent | 通过 |
| task-baseline 可推进到 P5-05 | 通过 |

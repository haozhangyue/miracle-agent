# 37_P5-01真实工作区盘点报告

> 任务状态：P5-01 已完成。  
> 盘点范围：`/Users/zhangyue/Documents/Obsidian Vault/热点工具更新` 中的真实工作流配置和 `runs/real` 交付包。  
> 盘点目标：确认真实 Flow A-G 接入 Miracle 前的样本、控制文件、产物文件、审核文件和缺口。

## 1. 结论

W24 交付包适合作为 P5 历史 Run 只读导入的主样本；W23 交付包适合作为对照样本，用于验证 importer 面对早期交付包和缺失控制文件时的降级规则。

关键判断：

- W24 具备 `phase_status.md`、`approval_log.md`、`task_trace.json`、`task_events.jsonl`、`approval_decisions.jsonl` 和 `render_manifest.json`，可以支撑 Trace、Gate、Artifact 和 Run projection 映射。
- W23 只有基础交付物和审计日志，缺少标准化 `phase_status.md`、`approval_log.md`、`task_trace.json`、`task_events.jsonl`、`approval_decisions.jsonl` 和 `render_manifest.json`，不能作为第一主样本。
- W24 的 `task_trace.json` 顶层结构是 `artifacts + steps`，不是 Miracle 的 RunSpec；P5-02 必须设计映射层，不能直接把它当 RunSpec 导入。
- W24 的 `phase_status.md` 只覆盖 C0/C/D/E/F，A/B/G 存在产物但缺标准状态行；P5-03 需要区分“真实事件”和“由文件存在推导出的 projection”。

## 2. 工作流配置盘点

| 资料 | 状态 | 说明 |
|---|---|---|
| `docs/overview/11_一页式工作流调用地图.md` | 已确认 | 定义 Flow A-G 作业调用链路和交付价值文件。 |
| `docs/overview/00_总控导航与执行地图.md` | 已确认 | 定义每周工作流、Agent 角色、交接规则和默认审核口径。 |
| `config/workflow/approval_policy.yaml` | 已确认 | 定义阶段审核模式、下游读取规则、required_outputs 和控制文件模板。 |

`approval_policy.yaml` 中的阶段是 P5-02 的映射基础：

| phase | name | review_mode | owner_agent |
|---|---|---|---|
| `A_fact_intelligence` | 情报采集与事实核验 | auto | 情报 Agent / 清洗 Agent / 核验 Agent |
| `B_md_master` | 内容 MD 母稿 | manual | 内容 Agent / 审核 Agent |
| `C0_script_pool_selection` | 脚本池生成与评审 | auto | 口播 Agent / 脚本评审 Agent |
| `C_ppt_storyboard` | PPT 与视频分镜 | manual | PPT Agent / 分镜 Agent / 审核 Agent |
| `D_voiceover_audio` | 配音脚本与声音生成 | manual | 配音 Agent / TTS字幕 Agent / 审核 Agent |
| `E_visual_video` | HyperFrames 视觉视频 | conditional | 视觉视频 Agent / 审核 Agent |
| `F_final_render` | 音画整合与最终渲染 | manual | 音画整合 Agent / 视频 Agent / 审核 Agent |
| `G_distribution_retro` | 分发包与复盘 | conditional | 分发 Agent / 复盘 Agent |

## 3. W24 主样本盘点

主样本路径：

```text
/Users/zhangyue/Documents/Obsidian Vault/热点工具更新/runs/real/2026-W24_Codex_ClaudeCode_真实任务交付包
```

文件规模：

| 指标 | 数量 |
|---|---:|
| 总文件数 | 140 |
| 控制文件数 | 9 |
| 媒体/预览文件数 | 56 |
| `00_任务控制` | 9 |
| `01_采集归档` | 1 |
| `02_清洗核验` | 1 |
| `03_内容母稿` | 21 |
| `04_PPT视频` | 105 |
| `05_平台分发` | 1 |
| `06_质检复盘` | 1 |

关键控制文件：

| 文件 | 存在 | P5 映射用途 |
|---|---|---|
| `00_任务控制/phase_status.md` | 是 | NodeRun/Gate 状态 projection。 |
| `00_任务控制/approval_log.md` | 是 | GateDecision 人读证据。 |
| `00_任务控制/task_trace.json` | 是 | NodeAttempt、Artifact、tool call、issue 映射。 |
| `00_任务控制/task_events.jsonl` | 是 | TraceEvent 映射。 |
| `00_任务控制/approval_decisions.jsonl` | 是 | GateDecision 结构化映射。 |
| `04_PPT视频/render_manifest.json` | 是 | 视频 ArtifactManifest、媒体校验和发布门映射。 |

`task_trace.json` 摘要：

| 字段 | 值 |
|---|---|
| run_id | `2026-W24_Codex_ClaudeCode` |
| title | Codex 与 Claude Code W24 正式音画整合与最终渲染 |
| date | `2026-06-12` |
| mode | `formal_audio_video_final_render` |
| total_duration_min | 28 |
| artifact_count | 41 |
| step_count | 11 |

`task_events.jsonl` 与审核事件：

| 文件 | 行数 | 说明 |
|---|---:|---|
| `task_events.jsonl` | 27 | 包含 step_start、step_end、issue、approval_decision 等运行事件。 |
| `approval_decisions.jsonl` | 2 | 包含 C_ppt_storyboard、D_voiceover_audio 的人工 approve 事件。 |

`phase_status.md` 当前覆盖：

| phase | status | 说明 |
|---|---|---|
| `C0_script_pool_selection` | `auto-approved` | 脚本池评审通过。 |
| `C_ppt_storyboard` | `approved` | 分镜草案已人工审核通过。 |
| `D_voiceover_audio` | `approved` | 新版 19 段配音听审通过。 |
| `E_visual_video` | `approved` | 横竖版 HyperFrames 工程桥接通过。 |
| `F_final_render` | `pending_review` | v3 横竖版 MP4 已渲染，等待人工视觉审看。 |

未覆盖但存在产物的阶段：

| phase | 产物证据 | P5 处理方式 |
|---|---|---|
| `A_fact_intelligence` | `01_采集归档/raw_items.md`、`02_清洗核验/clean_events.md` | 只能生成 Artifact projection，不能伪造 auto-approved TraceEvent。 |
| `B_md_master` | `03_内容母稿/MD母稿_公众号知乎版.md` | 只能生成 Artifact projection，是否等价 approved 需 P5-02 规则确认。 |
| `G_distribution_retro` | `05_平台分发/全平台发布包.md`、`06_质检复盘/发布前质检与复盘.md` | 只能生成 Artifact projection，发布状态需单独映射。 |

`render_manifest.json` 摘要：

| 字段 | 值 |
|---|---|
| status | `completed_pending_human_visual_review` |
| publish_allowed | `pending_human_visual_review` |
| 输出 | `horizontal_4k_v3`、`vertical_4k_v3` |
| 横版 | `3840x2160`，H.264，AAC 48kHz，预览帧存在 |
| 竖版 | `2160x3840`，H.264，AAC 48kHz，预览帧存在 |

## 4. W23 对照样本盘点

对照样本路径：

```text
/Users/zhangyue/Documents/Obsidian Vault/热点工具更新/runs/real/2026-W23_Codex_ClaudeCode_真实任务交付包
```

文件规模：

| 指标 | 数量 |
|---|---:|
| 总文件数 | 29 |
| 控制文件数 | 3 |
| 媒体/预览文件数 | 4 |
| `00_任务控制` | 3 |
| `01_采集归档` | 1 |
| `02_清洗核验` | 1 |
| `03_内容母稿` | 1 |
| `04_PPT视频` | 21 |
| `05_平台分发` | 1 |
| `06_质检复盘` | 1 |

缺失的标准控制文件：

| 文件 | 状态 | P5 影响 |
|---|---|---|
| `00_任务控制/phase_status.md` | 缺失 | 不能直接还原 NodeRun/Gate 状态。 |
| `00_任务控制/approval_log.md` | 缺失 | 不能直接还原人工审核历史。 |
| `00_任务控制/task_trace.json` | 缺失 | 不能还原 NodeAttempt/tool call。 |
| `00_任务控制/task_events.jsonl` | 缺失 | 不能还原 TraceEvent。 |
| `00_任务控制/approval_decisions.jsonl` | 缺失 | 不能结构化还原 GateDecision。 |
| `04_PPT视频/render_manifest.json` | 缺失 | 只能从文件名推导视频产物，不能还原 ffprobe 级校验。 |

W23 的价值是验证 importer 降级能力：当控制事件缺失时，Miracle 只能展示 ArtifactBoard 和基础 Run projection，不能展示完整事件时间线。

## 5. Miracle 对象候选映射

| Miracle 对象 | W24 来源 | 可信度 |
|---|---|---|
| DomainPack | `content-production`，来自工作流地图和审批策略 | 高 |
| WorkflowSpec | `approval_policy.yaml` phases + Flow A-G 文档 | 高 |
| RunSpec projection | W24 交付包路径、`task_trace.json` run_id/title/date/mode | 高 |
| WorkflowSnapshot | P5-02 生成的 `content-production-real` 映射结果 | 待设计 |
| NodeRun projection | `phase_status.md` 中 C0/C/D/E/F 状态 | 高 |
| NodeAttempt | `task_trace.json.steps` | 高 |
| TraceEvent | `task_events.jsonl` | 高 |
| GateSpec | `approval_policy.yaml` review_mode/manual_gate | 高 |
| GateInstance | `phase_status.md` pending_review/approved + required_outputs | 中 |
| GateDecision | `approval_decisions.jsonl` | 高 |
| ArtifactManifest | `task_trace.json.artifacts` + 交付物索引 + 实际文件 | 高 |
| AttentionItem | `F_final_render pending_review`、已知 warnings、缺失控制状态 | 中 |
| AgentHealth projection | `owner_agent`、`task_trace.json.steps[].agent` | 中 |

## 6. 缺口与风险

| 缺口 | 影响 | P5 处理建议 |
|---|---|---|
| W24 缺 A/B/G 标准 phase status | 早期阶段不能作为真实 NodeRun 状态导入 | 只生成 Artifact projection，并在 UI 标记为 inferred。 |
| W23 缺标准控制文件 | 不能做完整 Run 导入 | 仅作为降级对照样本。 |
| `task_trace.json` 不是 RunSpec | 不能直接复用 P4 fixture 模型 | P5-02 设计明确转换器和字段映射。 |
| 媒体文件数量多且不应进 Git | 不能复制进 Miracle fixture | 只记录本地路径、类型、大小、hash/mtime 和预览能力。 |
| `.DS_Store` 存在于交付包 | 不应进入 Miracle 导入对象 | importer 需要忽略系统文件。 |
| `F_final_render` 仍 pending_review | P5 UI 应展示真实 Gate 待审状态 | 映射为 GateInstance `pending_review` 和 AttentionItem。 |

## 7. P5-02 输入要求

`P5-02 Flow A-G 对象映射` 应基于本报告继续做三件事：

1. 定义 `content-production-real` WorkflowSpec 映射表，覆盖 A/B/C0/C/D/E/F/G。
2. 定义 W24 historical run importer 的输入输出：读取真实交付包，输出 Miracle Run projection。
3. 明确可信度字段：`observed_from_event`、`observed_from_status`、`inferred_from_file` 三类必须区分。

## 8. P5-01 验收

| 验收项 | 结果 |
|---|---|
| 确认真实 workflow policy | 通过 |
| 确认 W24 主样本 | 通过 |
| 确认 W23 对照样本 | 通过 |
| 确认 W24 控制文件 | 通过 |
| 确认 W23 缺口 | 通过 |
| 确认下一步对象映射输入 | 通过 |
| task-baseline 可推进到 P5-02 | 通过 |

{
  "name": "@miracle/sidecar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@miracle/core": "file:../../packages/core",
    "tsx": "^4.20.6"
  },
  "devDependencies": {
    "@types/node": "^24.10.2"
  }
}
